import { PublicKey, Signer, TransactionInstruction } from '@solana/web3.js';
import { Program, BN } from '@coral-xyz/anchor';
import { VolumeCurveBreakpoint, Settlement } from '../idlTypes';
/**
 * EscrowModule provides methods for managing escrow V1 and V2 accounts
 * on the Synapse Agent Protocol (SAP). Includes migration helpers.
 *
 * @deprecated V1 methods are deprecated; use V2 equivalents for new deployments.
 */
export declare class EscrowModule {
    private program;
    constructor(program: Program);
    /**
     * Close an escrow V2 account and reclaim rent.
     * @description Permanently closes an escrow V2 PDA, transferring remaining rent to the depositor.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.escrow - Escrow V2 PDA to close
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    closeEscrowV2(ctx: {
        signer: Signer;
        depositor: PublicKey;
        escrow: PublicKey;
        agentStats: PublicKey;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * Create a new escrow V2 account.
     * @description Initializes an escrow V2 PDA with pricing, volume curve, and dispute configuration.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStake - Agent stake PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.pricingMenu - Pricing menu PDA
     * @param {PublicKey} ctx.escrow - Escrow V2 PDA (derived from agent + depositor + nonce)
     * @param {BN} ctx.escrowNonce - Unique escrow nonce
     * @param {BN} ctx.pricePerCall - Price per call in lamports
     * @param {BN} ctx.maxCalls - Maximum calls allowed
     * @param {BN} ctx.initialDeposit - Initial deposit in lamports
     * @param {BN} ctx.expiresAt - Expiration timestamp
     * @param {VolumeCurveBreakpoint[]} ctx.volumeCurve - Volume-based pricing breakpoints
     * @param {PublicKey|null} ctx.tokenMint - Optional SPL token mint (null = SOL)
     * @param {number} ctx.tokenDecimals - Token decimals (0 for SOL)
     * @param {number} ctx.settlementSecurity - Settlement security level (0=Instant, 1=CoSigned, 2=DisputeWindow)
     * @param {BN} ctx.disputeWindowSlots - Dispute window duration in slots
     * @param {PublicKey|null} ctx.coSigner - Optional co-signer public key
     * @param {PublicKey|null} ctx.arbiter - Optional arbiter public key
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    createEscrowV2(ctx: {
        signer: Signer;
        depositor: PublicKey;
        agent: PublicKey;
        agentStake: PublicKey;
        agentStats: PublicKey;
        pricingMenu: PublicKey;
        escrow: PublicKey;
        escrowNonce: BN;
        pricePerCall: BN;
        maxCalls: BN;
        initialDeposit: BN;
        expiresAt: BN;
        volumeCurve: VolumeCurveBreakpoint[];
        tokenMint: (PublicKey | null);
        tokenDecimals: number;
        settlementSecurity: number;
        disputeWindowSlots: BN;
        coSigner: (PublicKey | null);
        arbiter: (PublicKey | null);
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * Deposit funds into an escrow V2 account.
     * @description Adds lamports or SPL tokens to an existing escrow V2 PDA.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.escrow - Escrow V2 PDA
     * @param {BN} ctx.escrowNonce - Escrow nonce for PDA derivation
     * @param {BN} ctx.amount - Amount to deposit in lamports (or token units)
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    depositEscrowV2(ctx: {
        signer: Signer;
        depositor: PublicKey;
        escrow: PublicKey;
        escrowNonce: BN;
        amount: BN;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * Finalize a pending settlement.
     * @description Transfers funds from escrow to agent wallet after dispute window expires.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (anyone can finalize)
     * @param {PublicKey} ctx.payer - Transaction fee payer
     * @param {PublicKey} ctx.agentWallet - Agent wallet receiving funds
     * @param {PublicKey} ctx.escrow - Escrow V2 PDA
     * @param {PublicKey} ctx.pendingSettlement - Pending settlement PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    finalizeSettlement(ctx: {
        signer: Signer;
        payer: PublicKey;
        agentWallet: PublicKey;
        escrow: PublicKey;
        pendingSettlement: PublicKey;
        agentStats: PublicKey;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * Settle calls for an escrow V2 account.
     * @description Records served calls and creates a settlement receipt for the agent.
     * Protocol fee: 0.5% of settlement amount automatically sent to treasury.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent wallet)
     * @param {PublicKey} ctx.wallet - Agent wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.escrow - Escrow V2 PDA
     * @param {PublicKey} ctx.settlementReceipt - Settlement receipt PDA
     * @param {BN} ctx.escrowNonce - Escrow nonce for PDA derivation
     * @param {BN} ctx.callsToSettle - Number of calls to settle
     * @param {number[]} ctx.serviceHash - SHA-256 hash proving service delivery
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    settleCallsV2(ctx: {
        signer: Signer;
        wallet: PublicKey;
        agent: PublicKey;
        agentStats: PublicKey;
        escrow: PublicKey;
        settlementReceipt: PublicKey;
        escrowNonce: BN;
        callsToSettle: BN;
        serviceHash: number[];
    }): Promise<TransactionInstruction>;
    /**
     * Withdraw funds from an escrow V2 account.
     * @description Allows depositor to withdraw available balance from escrow before settlement.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.escrow - Escrow V2 PDA
     * @param {BN} ctx.amount - Amount to withdraw in lamports
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    withdrawEscrowV2(ctx: {
        signer: Signer;
        depositor: PublicKey;
        escrow: PublicKey;
        amount: BN;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * Migrate escrow from V1 to V2.
     * @description Migrates an existing escrow V1 account to the V2 schema with enhanced features.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (escrow owner)
     * @param {PublicKey} ctx.wallet - Escrow owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.escrowV1 - Escrow V1 PDA to migrate from
     * @param {PublicKey} ctx.escrowV2 - Escrow V2 PDA to migrate to
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    migrateEscrowV1ToV2(ctx: {
        signer: Signer;
        wallet: PublicKey;
        agent: PublicKey;
        escrowV1: PublicKey;
        escrowV2: PublicKey;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * Settle multiple escrows in batch.
     * @description Processes multiple settlement entries in a single transaction for high-volume merchants.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent wallet)
     * @param {PublicKey} ctx.wallet - Agent wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {Settlement[]} ctx.settlements - Array of settlement entries (calls_to_settle, service_hash)
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    settleBatch(ctx: {
        signer: Signer;
        wallet: PublicKey;
        agent: PublicKey;
        agentStats: PublicKey;
        settlements: Settlement[];
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * @deprecated Use createEscrowV2() instead.
     * @description Creates a legacy escrow V1 PDA (SOL only, no dispute window).
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.pricingMenu - Pricing menu PDA
     * @param {PublicKey} ctx.escrow - Escrow V1 PDA
     * @param {BN} ctx.pricePerCall - Price per call in lamports
     * @param {BN} ctx.maxCalls - Maximum calls allowed
     * @param {BN} ctx.initialDeposit - Initial deposit in lamports
     * @param {BN} ctx.expiresAt - Expiration timestamp
     * @param {VolumeCurveBreakpoint[]} ctx.volumeCurve - Volume-based pricing breakpoints
     * @param {PublicKey|null} ctx.tokenMint - Optional SPL token mint (null = SOL)
     * @param {number} ctx.tokenDecimals - Token decimals (0 for SOL)
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    createEscrow(ctx: {
        signer: Signer;
        depositor: PublicKey;
        agent: PublicKey;
        agentStats: PublicKey;
        pricingMenu: PublicKey;
        escrow: PublicKey;
        pricePerCall: BN;
        maxCalls: BN;
        initialDeposit: BN;
        expiresAt: BN;
        volumeCurve: VolumeCurveBreakpoint[];
        tokenMint: (PublicKey | null);
        tokenDecimals: number;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * @deprecated Use depositEscrowV2() instead.
     * @description Deposits SOL into a legacy escrow V1 PDA.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.escrow - Escrow V1 PDA
     * @param {BN} ctx.amount - Amount to deposit in lamports
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    depositEscrow(ctx: {
        signer: Signer;
        depositor: PublicKey;
        escrow: PublicKey;
        amount: BN;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * @deprecated Use settleCallsV2() instead.
     * @description Records served calls for a legacy escrow V1 account.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent wallet)
     * @param {PublicKey} ctx.wallet - Agent wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.escrow - Escrow V1 PDA
     * @param {BN} ctx.callsToSettle - Number of calls to settle
     * @param {number[]} ctx.serviceHash - SHA-256 hash proving service delivery
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    settleCalls(ctx: {
        signer: Signer;
        wallet: PublicKey;
        agent: PublicKey;
        agentStats: PublicKey;
        escrow: PublicKey;
        callsToSettle: BN;
        serviceHash: number[];
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * @deprecated Use withdrawEscrowV2() instead.
     * @description Withdraws SOL from a legacy escrow V1 account.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.escrow - Escrow V1 PDA
     * @param {BN} ctx.amount - Amount to withdraw in lamports
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    withdrawEscrow(ctx: {
        signer: Signer;
        depositor: PublicKey;
        escrow: PublicKey;
        amount: BN;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * @deprecated Use closeEscrowV2() instead.
     * @description Closes a legacy escrow V1 PDA and reclaims rent.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (depositor)
     * @param {PublicKey} ctx.depositor - Escrow depositor's wallet
     * @param {PublicKey} ctx.escrow - Escrow V1 PDA to close
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    closeEscrow(ctx: {
        signer: Signer;
        depositor: PublicKey;
        escrow: PublicKey;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
    /**
     * @deprecated Use autoResolveDispute() or the new trustless flow instead.
     * @description Legacy arbiter-based dispute resolution for V1 escrows.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (arbiter)
     * @param {PublicKey} ctx.arbiter - Arbiter's wallet
     * @param {PublicKey} ctx.depositor - Depositor's wallet
     * @param {PublicKey} ctx.agentWallet - Agent wallet
     * @param {PublicKey} ctx.escrow - Escrow V1 PDA
     * @param {PublicKey} ctx.pendingSettlement - Pending settlement PDA
     * @param {BN} ctx.outcome - Dispute outcome (0=Pending, 1=AutoReleased, 2=DepositorWins, 3=AgentWins, 4=PartialRefund, 5=Split)
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts for stake slashing
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    resolveDispute(ctx: {
        signer: Signer;
        arbiter: PublicKey;
        depositor: PublicKey;
        agentWallet: PublicKey;
        escrow: PublicKey;
        pendingSettlement: PublicKey;
        outcome: BN;
        remainingAccounts?: any[];
    }): Promise<TransactionInstruction>;
}
//# sourceMappingURL=escrow.d.ts.map