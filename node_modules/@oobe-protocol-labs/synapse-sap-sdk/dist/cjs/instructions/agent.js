"use strict";
// ===============================================================
//  Agent Module — IDL v0.18.0
//  7 instructions
// ===============================================================
Object.defineProperty(exports, "__esModule", { value: true });
exports.AgentModule = void 0;
const web3_js_1 = require("@solana/web3.js");
const treasury_1 = require("../constants/treasury");
/**
 * AgentModule provides methods for managing agent lifecycle on the
 * Synapse Agent Protocol (SAP) v0.18.0.
 */
class AgentModule {
    program;
    constructor(program) {
        this.program = program;
    }
    /**
     * Close an agent account and reclaim rent.
     * @description Permanently closes an agent PDA, transferring remaining rent to the wallet.
     * Protocol fee: 0.05 SOL automatically sent to treasury.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA to close
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.vaultCheck - Vault check PDA
     * @param {PublicKey} ctx.pricingMenu - Pricing menu PDA
     * @param {PublicKey} ctx.globalRegistry - Global registry PDA
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async closeAgent(ctx) {
        return this.program
            .methods.closeAgent()
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            agentStats: ctx.agentStats,
            vaultCheck: ctx.vaultCheck,
            pricingMenu: ctx.pricingMenu,
            globalRegistry: ctx.globalRegistry,
        })
            .remainingAccounts([
            { pubkey: treasury_1.TREASURY_WALLET, isWritable: true, isSigner: false }
        ])
            .signers([ctx.signer])
            .instruction();
    }
    /**
     * Deactivate an agent account.
     * @description Sets agent is_active flag to false, hiding it from discovery indexes.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA to deactivate
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.globalRegistry - Global registry PDA
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async deactivateAgent(ctx) {
        return this.program
            .methods.deactivateAgent()
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            agentStats: ctx.agentStats,
            globalRegistry: ctx.globalRegistry,
        })
            .remainingAccounts(ctx.remainingAccounts ?? [])
            .signers([ctx.signer])
            .instruction();
    }
    /**
     * Reactivate a previously deactivated agent.
     * @description Sets agent is_active flag back to true, restoring visibility in indexes.
     * @param {Object} ctx - Context object containing required accounts and signers
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA to reactivate
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.globalRegistry - Global registry PDA
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async reactivateAgent(ctx) {
        return this.program
            .methods.reactivateAgent()
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            agentStats: ctx.agentStats,
            globalRegistry: ctx.globalRegistry,
        })
            .remainingAccounts(ctx.remainingAccounts ?? [])
            .signers([ctx.signer])
            .instruction();
    }
    /**
     * Register a new agent on the SAP.
     * @description Creates a new agent PDA with metadata, capabilities, pricing, and protocols.
     * Protocol fee: 0.1 SOL automatically sent to treasury.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA (derived from wallet)
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {PublicKey} ctx.globalRegistry - Global registry PDA
     * @param {string} ctx.name - Agent display name
     * @param {string} ctx.description - Agent description
     * @param {Capability[]} ctx.capabilities - Agent capabilities list
     * @param {PricingTier[]} ctx.pricing - Pricing tiers configuration
     * @param {string[]} ctx.protocols - Supported protocols
     * @param {string|null} ctx.agentId - Optional unique agent identifier
     * @param {string|null} ctx.agentUri - Optional agent metadata URI
     * @param {string|null} ctx.x402Endpoint - Optional x402 payment endpoint
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async registerAgent(ctx) {
        return this.program
            .methods.registerAgent(ctx.name, ctx.description, ctx.capabilities, ctx.pricing, ctx.protocols, ctx.agentId, ctx.agentUri, ctx.x402Endpoint)
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            agentStats: ctx.agentStats,
            globalRegistry: ctx.globalRegistry,
            systemProgram: web3_js_1.SystemProgram.programId,
        })
            .remainingAccounts([
            { pubkey: treasury_1.TREASURY_WALLET, isWritable: true, isSigner: false }
        ])
            .signers([ctx.signer])
            .instruction();
    }
    /**
     * Update agent metadata and configuration.
     * @description Partially updates agent fields; null values remain unchanged.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA to update
     * @param {PublicKey} ctx.pricingMenu - Pricing menu PDA
     * @param {string|null} ctx.name - New name (null = unchanged)
     * @param {string|null} ctx.description - New description (null = unchanged)
     * @param {Capability[]|null} ctx.capabilities - New capabilities (null = unchanged)
     * @param {PricingTier[]|null} ctx.pricing - New pricing (null = unchanged)
     * @param {string[]|null} ctx.protocols - New protocols (null = unchanged)
     * @param {string|null} ctx.agentId - New agent ID (null = unchanged)
     * @param {string|null} ctx.agentUri - New agent URI (null = unchanged)
     * @param {string|null} ctx.x402Endpoint - New x402 endpoint (null = unchanged)
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async updateAgent(ctx) {
        return this.program
            .methods.updateAgent(ctx.name, ctx.description, ctx.capabilities, ctx.pricing, ctx.protocols, ctx.agentId, ctx.agentUri, ctx.x402Endpoint)
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            pricingMenu: ctx.pricingMenu,
            systemProgram: web3_js_1.SystemProgram.programId,
        })
            .remainingAccounts(ctx.remainingAccounts ?? [])
            .signers([ctx.signer])
            .instruction();
    }
    /**
     * Report total calls served by the agent.
     * @description Updates agent stats with cumulative calls served count.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {BN} ctx.callsServed - Total calls served count
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async reportCalls(ctx) {
        return this.program
            .methods.reportCalls(ctx.callsServed)
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            agentStats: ctx.agentStats,
            systemProgram: web3_js_1.SystemProgram.programId,
        })
            .remainingAccounts(ctx.remainingAccounts ?? [])
            .signers([ctx.signer])
            .instruction();
    }
    /**
     * Update agent reputation metrics.
     * @description Updates agent's average latency and uptime percentage.
     * @param {Object} ctx - Context object containing required accounts and parameters
     * @param {Signer} ctx.signer - Transaction signer (agent owner)
     * @param {PublicKey} ctx.wallet - Agent owner's wallet
     * @param {PublicKey} ctx.agent - Agent PDA
     * @param {PublicKey} ctx.agentStats - Agent stats PDA
     * @param {number} ctx.avgLatencyMs - Average response latency in milliseconds
     * @param {number} ctx.uptimePercent - Uptime percentage (0-100)
     * @param {Object[]} [ctx.remainingAccounts] - Optional remaining accounts
     * @returns {Promise<TransactionInstruction>} Transaction instruction
     */
    async updateReputation(ctx) {
        return this.program
            .methods.updateReputation(ctx.avgLatencyMs, ctx.uptimePercent)
            .accounts({
            wallet: ctx.wallet,
            agent: ctx.agent,
            agentStats: ctx.agentStats,
            systemProgram: web3_js_1.SystemProgram.programId,
        })
            .remainingAccounts(ctx.remainingAccounts ?? [])
            .signers([ctx.signer])
            .instruction();
    }
}
exports.AgentModule = AgentModule;
//# sourceMappingURL=agent.js.map