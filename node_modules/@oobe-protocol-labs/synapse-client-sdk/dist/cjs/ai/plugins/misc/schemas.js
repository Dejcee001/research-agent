"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.allMiscMethods = exports.sendArcadeMethods = exports.gibworkMethods = exports.coingeckoMethods = exports.pythMethods = exports.alldomainsMethods = exports.snsMethods = void 0;
/**
 * @module ai/plugins/misc/schemas
 * @description Misc Plugin — Zod schemas for utility & data protocols.
 *
 * Covers:
 *  - SNS            (3 methods — Solana Name Service: register, resolve, reverse-lookup)
 *  - Alldomains     (3 methods — multi-TLD domain registration + resolution)
 *  - Pyth           (3 methods — on-chain price feeds)
 *  - CoinGecko      (6 methods — market data, trending, gainers, pools)
 *  - GibWork        (3 methods — bounties & tasks)
 *  - Send Arcade    (2 methods — on-chain mini-games / referral rewards)
 *
 * Total: 20 methods
 *
 * @since 2.0.0
 */
const zod_1 = require("zod");
const shared_1 = require("../../tools/protocols/shared");
const zPubkey = zod_1.z.string().describe('Solana public key (base58)');
const zMint = zod_1.z.string().describe('Token mint address (base58)');
const zAmount = zod_1.z.string().describe('Raw token amount (smallest unit)');
const zTx = zod_1.z.string().describe('Base64-encoded serialized transaction');
/* ═══════════════════════════════════════════════════════════════
 *  1. SNS — Solana Name Service (.sol domains)
 * ═══════════════════════════════════════════════════════════════ */
const { register: regSns, methods: snsMethods } = (0, shared_1.createMethodRegistry)('sns');
exports.snsMethods = snsMethods;
regSns('registerDomain', zod_1.z.object({
    wallet: zPubkey,
    domain: zod_1.z.string().describe('Domain name without .sol suffix (e.g. "myname")'),
    space: zod_1.z.number().int().min(1000).optional().default(1000).describe('Storage space in bytes'),
}), zod_1.z.object({
    domainKey: zPubkey.describe('Domain account public key'),
    tx: zTx,
    registrationCost: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Register a .sol domain name on Solana Name Service.');
regSns('resolveDomain', zod_1.z.object({
    domain: zod_1.z.string().describe('Full domain name (e.g. "myname.sol")'),
}), zod_1.z.object({
    owner: zPubkey.nullable().describe('Wallet that owns this domain, null if unregistered'),
    domainKey: zPubkey,
    data: zod_1.z.string().optional().describe('Custom data stored in the domain record'),
}), 'Resolve a .sol domain to its owner wallet address.');
regSns('reverseLookup', zod_1.z.object({
    wallet: zPubkey.describe('Wallet address to reverse-lookup'),
}), zod_1.z.object({
    domains: zod_1.z.array(zod_1.z.string()).describe('All .sol domains owned by this wallet'),
    primaryDomain: zod_1.z.string().nullable().describe('Primary/favourite domain'),
}), 'Reverse-lookup all .sol domains owned by a wallet.');
/* ═══════════════════════════════════════════════════════════════
 *  2. Alldomains — Multi-TLD Domain Resolution
 * ═══════════════════════════════════════════════════════════════ */
const { register: regDomains, methods: alldomainsMethods } = (0, shared_1.createMethodRegistry)('alldomains');
exports.alldomainsMethods = alldomainsMethods;
regDomains('registerDomain', zod_1.z.object({
    wallet: zPubkey,
    domain: zod_1.z.string().describe('Full domain (e.g. "myname.abc", "trader.bonk")'),
    tld: zod_1.z.string().optional().describe('TLD if not included in domain string'),
}), zod_1.z.object({
    domainKey: zPubkey,
    tx: zTx,
    cost: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Register a domain on AllDomains (supports multiple TLDs: .abc, .bonk, .poor, etc.).');
regDomains('resolveDomain', zod_1.z.object({
    domain: zod_1.z.string().describe('Full domain to resolve'),
}), zod_1.z.object({
    owner: zPubkey.nullable(),
    tld: zod_1.z.string(),
    domainKey: zPubkey,
}), 'Resolve a multi-TLD domain to its owner wallet.');
regDomains('getOwnedDomains', zod_1.z.object({
    wallet: zPubkey,
    tld: zod_1.z.string().optional().describe('Filter by TLD'),
}), zod_1.z.object({
    domains: zod_1.z.array(zod_1.z.object({
        domain: zod_1.z.string(),
        tld: zod_1.z.string(),
        domainKey: zPubkey,
    })),
}), 'Get all domains owned by a wallet across all TLDs.');
/* ═══════════════════════════════════════════════════════════════
 *  3. Pyth — On-Chain Price Feeds
 * ═══════════════════════════════════════════════════════════════ */
const { register: regPyth, methods: pythMethods } = (0, shared_1.createMethodRegistry)('pyth');
exports.pythMethods = pythMethods;
regPyth('getPrice', zod_1.z.object({
    priceId: zod_1.z.string().describe('Pyth price feed ID (hex) or symbol (e.g. "SOL/USD")'),
}), zod_1.z.object({
    id: zod_1.z.string(),
    price: zod_1.z.number(),
    confidence: zod_1.z.number(),
    expo: zod_1.z.number(),
    publishTime: zod_1.z.number(),
    emaPrice: zod_1.z.number().optional(),
    emaConfidence: zod_1.z.number().optional(),
    status: zod_1.z.enum(['trading', 'halted', 'unknown']),
}), 'Get the current price from a Pyth on-chain oracle feed.');
regPyth('getPriceHistory', zod_1.z.object({
    priceId: zod_1.z.string(),
    period: zod_1.z.enum(['1h', '4h', '1d', '7d', '30d']).optional().default('1d'),
}), zod_1.z.object({
    prices: zod_1.z.array(zod_1.z.object({
        timestamp: zod_1.z.number(),
        price: zod_1.z.number(),
        confidence: zod_1.z.number(),
    })),
    symbol: zod_1.z.string().optional(),
}), 'Get historical price data from Pyth oracle.');
regPyth('listPriceFeeds', zod_1.z.object({
    query: zod_1.z.string().optional().describe('Search filter (e.g. "SOL", "BTC")'),
    assetType: zod_1.z.enum(['crypto', 'equity', 'fx', 'metal', 'rates']).optional(),
}), zod_1.z.object({
    feeds: zod_1.z.array(zod_1.z.object({
        id: zod_1.z.string(),
        symbol: zod_1.z.string(),
        assetType: zod_1.z.string(),
        description: zod_1.z.string().optional(),
    })),
}), 'List available Pyth price feeds, optionally filtered by query or asset type.');
/* ═══════════════════════════════════════════════════════════════
 *  4. CoinGecko — Market Data (Pro API)
 * ═══════════════════════════════════════════════════════════════ */
const { register: regGecko, methods: coingeckoMethods } = (0, shared_1.createMethodRegistry)('coingecko');
exports.coingeckoMethods = coingeckoMethods;
regGecko('getTokenPrice', zod_1.z.object({
    tokenId: zod_1.z.string().describe('CoinGecko token ID or contract address'),
    vsCurrency: zod_1.z.string().optional().default('usd'),
    includeMarketCap: zod_1.z.boolean().optional().default(true),
    include24hChange: zod_1.z.boolean().optional().default(true),
    include24hVolume: zod_1.z.boolean().optional().default(true),
}), zod_1.z.object({
    id: zod_1.z.string(),
    symbol: zod_1.z.string(),
    currentPrice: zod_1.z.number(),
    marketCap: zod_1.z.number().optional(),
    priceChange24h: zod_1.z.number().optional(),
    priceChangePercent24h: zod_1.z.number().optional(),
    volume24h: zod_1.z.number().optional(),
    high24h: zod_1.z.number().optional(),
    low24h: zod_1.z.number().optional(),
    ath: zod_1.z.number().optional(),
    athDate: zod_1.z.string().optional(),
}), 'Get token price and market data from CoinGecko.');
regGecko('getTrending', zod_1.z.object({}), zod_1.z.object({
    coins: zod_1.z.array(zod_1.z.object({
        id: zod_1.z.string(),
        name: zod_1.z.string(),
        symbol: zod_1.z.string(),
        marketCapRank: zod_1.z.number().optional(),
        priceChange24h: zod_1.z.number().optional(),
        thumb: zod_1.z.string().optional(),
    })),
    nfts: zod_1.z.array(zod_1.z.object({
        id: zod_1.z.string(),
        name: zod_1.z.string(),
        symbol: zod_1.z.string(),
        thumb: zod_1.z.string().optional(),
    })).optional(),
}), 'Get trending tokens and NFTs on CoinGecko.');
regGecko('getTopGainersLosers', zod_1.z.object({
    vsCurrency: zod_1.z.string().optional().default('usd'),
    duration: zod_1.z.enum(['1h', '24h', '7d', '14d', '30d', '1y']).optional().default('24h'),
}), zod_1.z.object({
    topGainers: zod_1.z.array(zod_1.z.object({
        id: zod_1.z.string(),
        symbol: zod_1.z.string(),
        name: zod_1.z.string(),
        priceChangePercent: zod_1.z.number(),
        currentPrice: zod_1.z.number(),
    })),
    topLosers: zod_1.z.array(zod_1.z.object({
        id: zod_1.z.string(),
        symbol: zod_1.z.string(),
        name: zod_1.z.string(),
        priceChangePercent: zod_1.z.number(),
        currentPrice: zod_1.z.number(),
    })),
}), 'Get top gainers and losers from CoinGecko.');
regGecko('getTokenInfo', zod_1.z.object({
    tokenId: zod_1.z.string().describe('CoinGecko token ID'),
}), zod_1.z.object({
    id: zod_1.z.string(),
    name: zod_1.z.string(),
    symbol: zod_1.z.string(),
    description: zod_1.z.string().optional(),
    homepage: zod_1.z.string().optional(),
    genesisDate: zod_1.z.string().optional(),
    categories: zod_1.z.array(zod_1.z.string()).optional(),
    platforms: zod_1.z.record(zod_1.z.string(), zod_1.z.string()).optional().describe('Chain → contract_address mapping'),
    links: zod_1.z.object({
        twitter: zod_1.z.string().optional(),
        telegram: zod_1.z.string().optional(),
        discord: zod_1.z.string().optional(),
        github: zod_1.z.array(zod_1.z.string()).optional(),
    }).optional(),
    marketData: zod_1.z.object({
        currentPrice: zod_1.z.record(zod_1.z.string(), zod_1.z.number()).optional(),
        totalSupply: zod_1.z.number().optional(),
        circulatingSupply: zod_1.z.number().optional(),
        maxSupply: zod_1.z.number().nullable().optional(),
        fdv: zod_1.z.number().optional(),
    }).optional(),
}), 'Get detailed token information from CoinGecko (description, links, market data).');
regGecko('getPoolsByToken', zod_1.z.object({
    network: zod_1.z.string().optional().default('solana'),
    tokenAddress: zod_1.z.string().describe('Token contract/mint address'),
    page: zod_1.z.number().int().min(1).optional().default(1),
}), zod_1.z.object({
    pools: zod_1.z.array(zod_1.z.object({
        poolAddress: zod_1.z.string(),
        dex: zod_1.z.string(),
        baseToken: zod_1.z.object({ symbol: zod_1.z.string(), address: zod_1.z.string() }),
        quoteToken: zod_1.z.object({ symbol: zod_1.z.string(), address: zod_1.z.string() }),
        priceUsd: zod_1.z.string().optional(),
        volume24h: zod_1.z.number().optional(),
        liquidity: zod_1.z.number().optional(),
        fdv: zod_1.z.number().optional(),
    })),
    totalPools: zod_1.z.number(),
}), 'Get all liquidity pools for a token from CoinGecko on-chain DEX tracker.');
regGecko('getOHLCV', zod_1.z.object({
    tokenId: zod_1.z.string(),
    vsCurrency: zod_1.z.string().optional().default('usd'),
    days: zod_1.z.enum(['1', '7', '14', '30', '90', '180', '365', 'max']).optional().default('7'),
}), zod_1.z.object({
    prices: zod_1.z.array(zod_1.z.tuple([zod_1.z.number(), zod_1.z.number()])).describe('[[timestamp, price], ...]'),
    marketCaps: zod_1.z.array(zod_1.z.tuple([zod_1.z.number(), zod_1.z.number()])).optional(),
    totalVolumes: zod_1.z.array(zod_1.z.tuple([zod_1.z.number(), zod_1.z.number()])).optional(),
}), 'Get OHLCV (price chart) data from CoinGecko.');
/* ═══════════════════════════════════════════════════════════════
 *  5. GibWork — Bounties & Tasks
 * ═══════════════════════════════════════════════════════════════ */
const { register: regGib, methods: gibworkMethods } = (0, shared_1.createMethodRegistry)('gibwork');
exports.gibworkMethods = gibworkMethods;
regGib('createBounty', zod_1.z.object({
    creator: zPubkey,
    title: zod_1.z.string().describe('Bounty title'),
    description: zod_1.z.string().describe('Detailed bounty description'),
    rewardMint: zMint.optional().describe('Reward token mint (default: SOL)'),
    rewardAmount: zAmount.describe('Reward amount'),
    tags: zod_1.z.array(zod_1.z.string()).optional(),
    deadline: zod_1.z.number().optional().describe('Deadline (Unix timestamp)'),
}), zod_1.z.object({
    bountyId: zod_1.z.string(),
    tx: zTx,
    escrowAddress: zPubkey,
    signature: zod_1.z.string().optional(),
}), 'Create a new bounty on Gib Work with on-chain escrow.');
regGib('listBounties', zod_1.z.object({
    status: zod_1.z.enum(['open', 'in_progress', 'completed', 'cancelled']).optional().default('open'),
    tags: zod_1.z.array(zod_1.z.string()).optional(),
    limit: zod_1.z.number().int().min(1).max(100).optional().default(20),
}), zod_1.z.object({
    bounties: zod_1.z.array(zod_1.z.object({
        bountyId: zod_1.z.string(),
        title: zod_1.z.string(),
        creator: zPubkey,
        rewardAmount: zAmount,
        rewardMint: zMint,
        status: zod_1.z.string(),
        tags: zod_1.z.array(zod_1.z.string()),
        deadline: zod_1.z.number().optional(),
    })),
    totalCount: zod_1.z.number(),
}), 'List available bounties on Gib Work.');
regGib('submitWork', zod_1.z.object({
    wallet: zPubkey,
    bountyId: zod_1.z.string(),
    submissionUrl: zod_1.z.string().url().describe('URL to the completed work (PR, doc, etc.)'),
    notes: zod_1.z.string().optional(),
}), zod_1.z.object({
    submissionId: zod_1.z.string(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Submit completed work for a Gib Work bounty.');
/* ═══════════════════════════════════════════════════════════════
 *  6. Send Arcade — On-Chain Mini-Games
 * ═══════════════════════════════════════════════════════════════ */
const { register: regArcade, methods: sendArcadeMethods } = (0, shared_1.createMethodRegistry)('send-arcade');
exports.sendArcadeMethods = sendArcadeMethods;
regArcade('listGames', zod_1.z.object({
    status: zod_1.z.enum(['active', 'upcoming', 'ended']).optional().default('active'),
}), zod_1.z.object({
    games: zod_1.z.array(zod_1.z.object({
        gameId: zod_1.z.string(),
        name: zod_1.z.string(),
        description: zod_1.z.string().optional(),
        entryFee: zAmount,
        entryFeeMint: zMint.optional(),
        prizePool: zAmount.optional(),
        playersCount: zod_1.z.number().optional(),
        status: zod_1.z.string(),
    })),
}), 'List available games on Send Arcade.');
regArcade('playGame', zod_1.z.object({
    wallet: zPubkey,
    gameId: zod_1.z.string(),
    betAmount: zAmount.optional().describe('Bet/entry amount (if variable)'),
}), zod_1.z.object({
    tx: zTx,
    sessionId: zod_1.z.string().optional(),
    entryFee: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Enter and play a game on Send Arcade (pays entry fee via transaction).');
exports.allMiscMethods = [
    ...snsMethods,
    ...alldomainsMethods,
    ...pythMethods,
    ...coingeckoMethods,
    ...gibworkMethods,
    ...sendArcadeMethods,
];
//# sourceMappingURL=schemas.js.map