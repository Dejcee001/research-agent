"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.allBlinksMethods = exports.blinksMethods = void 0;
/**
 * @module ai/plugins/blinks/schemas
 * @description Blinks Plugin — Zod schemas for Solana Actions (Blinks) protocol.
 *
 * Implements the Solana Actions spec (https://solana.com/docs/advanced/actions):
 *  - **blinks** (6 methods — discover, inspect, execute, and track Solana Actions / Blinks)
 *
 * @since 2.0.0
 */
const zod_1 = require("zod");
const shared_1 = require("../../tools/protocols/shared");
const zPubkey = zod_1.z.string().describe('Solana public key (base58)');
const zTx = zod_1.z.string().describe('Base64-encoded serialized transaction');
/* ═══════════════════════════════════════════════════════════════
 *  Blinks — Solana Actions
 * ═══════════════════════════════════════════════════════════════ */
const { register: regBlinks, methods: blinksMethods } = (0, shared_1.createMethodRegistry)('blinks');
exports.blinksMethods = blinksMethods;
regBlinks('getAction', zod_1.z.object({
    actionUrl: zod_1.z.string().url().describe('Solana Action URL (https endpoint implementing the Actions spec)'),
}), zod_1.z.object({
    type: zod_1.z.enum(['action', 'completed']).optional(),
    icon: zod_1.z.string().url().optional().describe('Icon URL for the Action'),
    title: zod_1.z.string().describe('Action title'),
    description: zod_1.z.string().describe('Action description'),
    label: zod_1.z.string().describe('Button label text'),
    disabled: zod_1.z.boolean().optional(),
    error: zod_1.z.object({ message: zod_1.z.string() }).optional(),
    links: zod_1.z.object({
        actions: zod_1.z.array(zod_1.z.object({
            href: zod_1.z.string().describe('Action endpoint URL (may contain template params)'),
            label: zod_1.z.string(),
            parameters: zod_1.z.array(zod_1.z.object({
                name: zod_1.z.string(),
                label: zod_1.z.string().optional(),
                required: zod_1.z.boolean().optional(),
                type: zod_1.z.enum(['text', 'number', 'email', 'url', 'date', 'select', 'textarea', 'radio', 'checkbox'])
                    .optional(),
                options: zod_1.z.array(zod_1.z.object({
                    label: zod_1.z.string(),
                    value: zod_1.z.string(),
                })).optional(),
                min: zod_1.z.number().optional(),
                max: zod_1.z.number().optional(),
                pattern: zod_1.z.string().optional(),
                patternDescription: zod_1.z.string().optional(),
            })).optional(),
        })).optional(),
    }).optional(),
}), 'Fetch metadata for a Solana Action (Blink) — returns title, description, available actions, and parameters.');
regBlinks('executeAction', zod_1.z.object({
    actionUrl: zod_1.z.string().url().describe('Action endpoint URL (from getAction links)'),
    wallet: zPubkey.describe('User wallet to sign the transaction'),
    params: zod_1.z.record(zod_1.z.string(), zod_1.z.string()).optional()
        .describe('Key-value parameters to fill Action template variables'),
}), zod_1.z.object({
    transaction: zTx.describe('Base64-encoded transaction to be signed'),
    message: zod_1.z.string().optional().describe('Human-readable message from the Action provider'),
    redirect: zod_1.z.string().url().optional().describe('Optional redirect URL after completion'),
}), 'Execute a Solana Action — sends wallet to the Action endpoint and receives a transaction to sign.');
regBlinks('confirmAction', zod_1.z.object({
    actionUrl: zod_1.z.string().url(),
    signature: zod_1.z.string().describe('Transaction signature after signing and submitting'),
}), zod_1.z.object({
    type: zod_1.z.literal('completed').optional(),
    icon: zod_1.z.string().url().optional(),
    title: zod_1.z.string().optional(),
    description: zod_1.z.string().optional(),
    label: zod_1.z.string().optional(),
    nextAction: zod_1.z.string().url().optional().describe('URL for a chained follow-up action'),
}), 'Confirm a completed Solana Action by sending the tx signature back to the Action provider.');
regBlinks('resolveBlinkUrl', zod_1.z.object({
    blinkUrl: zod_1.z.string().url().describe('Blink URL (e.g. https://dial.to/?action=solana-action:...)'),
}), zod_1.z.object({
    actionUrl: zod_1.z.string().url().describe('Resolved Solana Action URL'),
    provider: zod_1.z.string().optional().describe('Blink provider (dial.to, phantom, etc.)'),
    isInterstitial: zod_1.z.boolean().optional().describe('Whether this is a proxy/interstitial Blink URL'),
}), 'Resolve a Blink URL (e.g. dial.to link) to its underlying Solana Action URL.');
regBlinks('validateActionsJson', zod_1.z.object({
    domain: zod_1.z.string().describe('Domain to check for actions.json (e.g. "jupiter.exchange")'),
}), zod_1.z.object({
    valid: zod_1.z.boolean(),
    rules: zod_1.z.array(zod_1.z.object({
        pathPattern: zod_1.z.string(),
        apiPath: zod_1.z.string(),
    })).optional(),
    error: zod_1.z.string().optional(),
}), 'Validate a domain\'s actions.json file — checks if a website correctly implements the Solana Actions spec.');
regBlinks('buildActionUrl', zod_1.z.object({
    baseUrl: zod_1.z.string().url().describe('Action API endpoint'),
    params: zod_1.z.record(zod_1.z.string(), zod_1.z.string()).optional().describe('Template parameters to encode'),
    blinkProvider: zod_1.z.enum(['dial.to', 'phantom', 'backpack', 'none']).optional().default('none')
        .describe('Optionally wrap in a Blink provider URL'),
}), zod_1.z.object({
    actionUrl: zod_1.z.string().url().describe('Constructed Solana Action URL'),
    blinkUrl: zod_1.z.string().url().optional().describe('Blink-wrapped URL (if provider selected)'),
    shareable: zod_1.z.string().optional().describe('Short shareable URL'),
}), 'Build and optionally wrap a Solana Action URL for sharing as a Blink.');
exports.allBlinksMethods = [...blinksMethods];
//# sourceMappingURL=schemas.js.map