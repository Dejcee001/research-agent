"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.allNftMethods = exports.dasMethods = exports.threeLandMethods = exports.metaplexNftMethods = void 0;
/**
 * @module ai/plugins/nft/schemas
 * @description NFT Plugin — Zod schemas for NFT operations.
 *
 * Covers:
 *  - Metaplex NFT management     (collection deploy, mint, metadata, royalties)
 *  - 3.Land integration          (create collection, mint + auto-list, list for sale)
 *  - DAS queries                 (search, get by owner, get by creator)
 *
 * @since 2.0.0
 */
const zod_1 = require("zod");
const shared_1 = require("../../tools/protocols/shared");
const zPubkey = zod_1.z.string().describe('Solana public key (base58)');
const zMint = zod_1.z.string().describe('NFT mint address (base58)');
const zTx = zod_1.z.string().describe('Base64-encoded serialized transaction');
/* ═══════════════════════════════════════════════════════════════
 *  1. Metaplex NFT Management
 * ═══════════════════════════════════════════════════════════════ */
const { register: regMpl, methods: metaplexNftMethods } = (0, shared_1.createMethodRegistry)('metaplex-nft');
exports.metaplexNftMethods = metaplexNftMethods;
regMpl('deployCollection', zod_1.z.object({
    authority: zPubkey.describe('Collection authority wallet'),
    name: zod_1.z.string().describe('Collection name'),
    symbol: zod_1.z.string().max(10).describe('Collection symbol'),
    uri: zod_1.z.string().url().describe('URI to collection metadata JSON'),
    sellerFeeBasisPoints: zod_1.z.number().int().min(0).max(10000).default(500)
        .describe('Royalty fee in bps (500 = 5%)'),
    isMutable: zod_1.z.boolean().optional().default(true),
    maxSupply: zod_1.z.number().int().optional().describe('Max NFTs in collection (0 = unlimited)'),
    creators: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        share: zod_1.z.number().int().min(0).max(100).describe('Revenue share percentage'),
        verified: zod_1.z.boolean().optional().default(false),
    })).optional().describe('Creator list with revenue shares (must sum to 100)'),
}), zod_1.z.object({
    collectionMint: zMint.describe('Collection NFT mint address'),
    metadataAddress: zPubkey,
    masterEditionAddress: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Deploy a new NFT collection via Metaplex. Returns the collection mint and metadata addresses.');
regMpl('mintNFT', zod_1.z.object({
    authority: zPubkey.describe('Mint authority wallet'),
    collection: zMint.optional().describe('Collection mint to add this NFT to'),
    name: zod_1.z.string().describe('NFT name'),
    symbol: zod_1.z.string().max(10).optional().describe('NFT symbol'),
    uri: zod_1.z.string().url().describe('URI to NFT metadata JSON (image, attributes, etc.)'),
    sellerFeeBasisPoints: zod_1.z.number().int().min(0).max(10000).default(500),
    isMutable: zod_1.z.boolean().optional().default(true),
    creators: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        share: zod_1.z.number().int().min(0).max(100),
        verified: zod_1.z.boolean().optional().default(false),
    })).optional(),
    recipient: zPubkey.optional().describe('Recipient wallet (default: authority)'),
    isCompressed: zod_1.z.boolean().optional().default(false).describe('Mint as compressed NFT (cNFT) for lower cost'),
    merkleTree: zPubkey.optional().describe('Merkle tree account for compressed NFTs'),
}), zod_1.z.object({
    mint: zMint.describe('NFT mint address'),
    metadataAddress: zPubkey,
    editionAddress: zPubkey.optional(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Mint a new NFT via Metaplex. Supports both standard and compressed (cNFT) minting.');
regMpl('updateMetadata', zod_1.z.object({
    mint: zMint.describe('NFT mint address'),
    updateAuthority: zPubkey.describe('Current update authority'),
    name: zod_1.z.string().optional().describe('New name'),
    symbol: zod_1.z.string().optional().describe('New symbol'),
    uri: zod_1.z.string().url().optional().describe('New metadata URI'),
    sellerFeeBasisPoints: zod_1.z.number().int().min(0).max(10000).optional(),
    creators: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        share: zod_1.z.number().int().min(0).max(100),
        verified: zod_1.z.boolean().optional(),
    })).optional(),
    isMutable: zod_1.z.boolean().optional(),
    primarySaleHappened: zod_1.z.boolean().optional(),
    newUpdateAuthority: zPubkey.optional().describe('Transfer update authority to a new wallet'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Update NFT metadata fields (name, symbol, URI, creators, royalties, authority).');
regMpl('verifyCreator', zod_1.z.object({
    mint: zMint,
    creator: zPubkey.describe('Creator wallet to verify'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Verify a creator on an NFT\'s metadata (requires creator\'s signature).');
regMpl('verifyCollection', zod_1.z.object({
    mint: zMint.describe('NFT mint to verify'),
    collection: zMint.describe('Collection mint'),
    collectionAuthority: zPubkey.describe('Collection authority wallet'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Verify an NFT as part of a collection.');
regMpl('setAndVerifyCollection', zod_1.z.object({
    mint: zMint,
    collection: zMint,
    collectionAuthority: zPubkey,
    updateAuthority: zPubkey.describe('NFT update authority'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Set and verify a collection on an NFT in a single transaction.');
regMpl('delegateAuthority', zod_1.z.object({
    mint: zMint,
    currentAuthority: zPubkey,
    newAuthority: zPubkey,
    authorityType: zod_1.z.enum(['update', 'mint', 'freeze', 'collection']).describe('Type of authority to delegate'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Delegate a specific authority (update, mint, freeze, collection) to another wallet.');
regMpl('revokeAuthority', zod_1.z.object({
    mint: zMint,
    authority: zPubkey.describe('Current authority holder'),
    authorityType: zod_1.z.enum(['update', 'mint', 'freeze', 'collection']),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Revoke (remove) a specific authority. Irreversible for some types.');
regMpl('configureRoyalties', zod_1.z.object({
    mint: zMint,
    updateAuthority: zPubkey,
    sellerFeeBasisPoints: zod_1.z.number().int().min(0).max(10000).describe('Royalty fee in bps'),
    creators: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        share: zod_1.z.number().int().min(0).max(100),
    })).describe('Creator list with revenue shares (must sum to 100)'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Configure royalty fee and creator splits for an NFT.');
/* ═══════════════════════════════════════════════════════════════
 *  2. 3.Land NFT Marketplace
 * ═══════════════════════════════════════════════════════════════ */
const { register: reg3land, methods: threeLandMethods } = (0, shared_1.createMethodRegistry)('3land');
exports.threeLandMethods = threeLandMethods;
reg3land('createCollection', zod_1.z.object({
    owner: zPubkey.describe('Collection owner wallet'),
    name: zod_1.z.string().describe('Collection name'),
    symbol: zod_1.z.string().describe('Collection symbol'),
    description: zod_1.z.string().optional().describe('Collection description'),
    image: zod_1.z.string().url().describe('Collection cover image URL'),
    royaltyBps: zod_1.z.number().int().min(0).max(10000).default(500),
    creators: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        share: zod_1.z.number().int().min(0).max(100),
    })).optional(),
}), zod_1.z.object({
    collectionId: zod_1.z.string().describe('3.Land collection ID'),
    collectionMint: zMint,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a new NFT collection on 3.Land marketplace.');
reg3land('mintAndList', zod_1.z.object({
    owner: zPubkey,
    collection: zod_1.z.string().describe('3.Land collection ID'),
    name: zod_1.z.string(),
    description: zod_1.z.string().optional(),
    image: zod_1.z.string().url().describe('NFT image URL'),
    attributes: zod_1.z.array(zod_1.z.object({
        trait_type: zod_1.z.string(),
        value: zod_1.z.union([zod_1.z.string(), zod_1.z.number()]),
    })).optional(),
    price: zod_1.z.string().describe('Listing price in token amount'),
    priceMint: zMint.optional().describe('Price in this SPL token (default: SOL)'),
    supply: zod_1.z.number().int().positive().optional().default(1).describe('Edition supply'),
}), zod_1.z.object({
    mint: zMint,
    listingId: zod_1.z.string(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Mint an NFT on 3.Land and automatically list it for sale.');
reg3land('listForSale', zod_1.z.object({
    seller: zPubkey,
    mint: zMint.describe('NFT mint to list'),
    price: zod_1.z.string().describe('Listing price (in specified token)'),
    priceMint: zMint.optional().describe('SPL token for pricing (default: SOL). Any SPL token supported.'),
    expiry: zod_1.z.string().optional().describe('ISO 8601 listing expiry timestamp'),
}), zod_1.z.object({
    listingId: zod_1.z.string(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'List an NFT for sale on 3.Land in any SPL token.');
reg3land('cancelListing', zod_1.z.object({
    seller: zPubkey,
    listingId: zod_1.z.string().describe('3.Land listing ID to cancel'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Cancel an active NFT listing on 3.Land.');
reg3land('buyNFT', zod_1.z.object({
    buyer: zPubkey,
    listingId: zod_1.z.string().describe('3.Land listing ID'),
}), zod_1.z.object({
    mint: zMint,
    tx: zTx,
    price: zod_1.z.string(),
    priceMint: zMint,
    signature: zod_1.z.string().optional(),
}), 'Purchase an NFT from a 3.Land listing.');
/* ═══════════════════════════════════════════════════════════════
 *  3. DAS — Digital Asset Standard queries
 * ═══════════════════════════════════════════════════════════════ */
const { register: regDas, methods: dasMethods } = (0, shared_1.createMethodRegistry)('das');
exports.dasMethods = dasMethods;
regDas('getAsset', zod_1.z.object({
    id: zMint.describe('Asset mint address'),
}), zod_1.z.object({
    id: zMint,
    content: zod_1.z.object({
        json_uri: zod_1.z.string().optional(),
        metadata: zod_1.z.object({
            name: zod_1.z.string().optional(),
            symbol: zod_1.z.string().optional(),
            description: zod_1.z.string().optional(),
            image: zod_1.z.string().optional(),
            attributes: zod_1.z.array(zod_1.z.object({
                trait_type: zod_1.z.string(),
                value: zod_1.z.union([zod_1.z.string(), zod_1.z.number()]),
            })).optional(),
        }).optional(),
    }).optional(),
    authorities: zod_1.z.array(zod_1.z.unknown()).optional(),
    compression: zod_1.z.object({
        compressed: zod_1.z.boolean(),
        tree: zPubkey.optional(),
        leaf_id: zod_1.z.number().optional(),
    }).optional(),
    ownership: zod_1.z.object({
        owner: zPubkey,
        delegate: zPubkey.optional(),
        frozen: zod_1.z.boolean().optional(),
    }).optional(),
    royalty: zod_1.z.object({
        basis_points: zod_1.z.number().optional(),
        primary_sale_happened: zod_1.z.boolean().optional(),
    }).optional(),
}), 'Get detailed information about a single NFT/asset via DAS.');
regDas('getAssetsByOwner', zod_1.z.object({
    ownerAddress: zPubkey,
    page: zod_1.z.number().int().positive().optional().default(1),
    limit: zod_1.z.number().int().min(1).max(1000).optional().default(100),
    sortBy: zod_1.z.enum(['created', 'updated', 'recent_action']).optional(),
    before: zod_1.z.string().optional(),
    after: zod_1.z.string().optional(),
}), zod_1.z.object({
    items: zod_1.z.array(zod_1.z.unknown()),
    total: zod_1.z.number(),
    page: zod_1.z.number(),
    limit: zod_1.z.number(),
}), 'Get all NFTs/assets owned by a wallet via DAS (supports pagination).');
regDas('getAssetsByCreator', zod_1.z.object({
    creatorAddress: zPubkey,
    onlyVerified: zod_1.z.boolean().optional().default(true),
    page: zod_1.z.number().int().positive().optional().default(1),
    limit: zod_1.z.number().int().min(1).max(1000).optional().default(100),
}), zod_1.z.object({
    items: zod_1.z.array(zod_1.z.unknown()),
    total: zod_1.z.number(),
    page: zod_1.z.number(),
}), 'Get all NFTs/assets created by a specific creator via DAS.');
regDas('getAssetsByCollection', zod_1.z.object({
    collectionAddress: zMint,
    page: zod_1.z.number().int().positive().optional().default(1),
    limit: zod_1.z.number().int().min(1).max(1000).optional().default(100),
}), zod_1.z.object({
    items: zod_1.z.array(zod_1.z.unknown()),
    total: zod_1.z.number(),
    page: zod_1.z.number(),
}), 'Get all NFTs in a collection via DAS.');
regDas('searchAssets', zod_1.z.object({
    ownerAddress: zPubkey.optional(),
    creatorAddress: zPubkey.optional(),
    grouping: zod_1.z.tuple([zod_1.z.string(), zod_1.z.string()]).optional().describe('Grouping key-value pair (e.g. ["collection", "mint_address"])'),
    compressed: zod_1.z.boolean().optional(),
    frozen: zod_1.z.boolean().optional(),
    burnt: zod_1.z.boolean().optional().default(false),
    jsonUri: zod_1.z.string().optional(),
    page: zod_1.z.number().int().positive().optional().default(1),
    limit: zod_1.z.number().int().min(1).max(1000).optional().default(100),
}), zod_1.z.object({
    items: zod_1.z.array(zod_1.z.unknown()),
    total: zod_1.z.number(),
    page: zod_1.z.number(),
}), 'Search NFTs/assets with flexible filters via DAS.');
exports.allNftMethods = [...metaplexNftMethods, ...threeLandMethods, ...dasMethods];
//# sourceMappingURL=schemas.js.map