"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.allTokenMethods = exports.tokenBridgingMethods = exports.tokenStakingMethods = exports.tokenSplMethods = void 0;
/**
 * @module ai/plugins/token/schemas
 * @description Token Plugin — Zod schemas for SPL token operations.
 *
 * Covers:
 *  - SPL Token operations    (deploy, transfer, burn, freeze, mint-to, close)
 *  - Balance checks          (token + SOL native)
 *  - Token metadata          (Metaplex token-metadata lookups)
 *  - Staking                 (native SOL staking, JupSOL, Solayer sSOL)
 *  - Bridging                (Wormhole cross-chain, deBridge DLN)
 *  - Rug / safety checks     (token security analysis)
 *
 * @since 2.0.0
 */
const zod_1 = require("zod");
const shared_1 = require("../../tools/protocols/shared");
/* ═══════════════════════════════════════════════════════════════
 *  Shared primitives
 * ═══════════════════════════════════════════════════════════════ */
const zMint = zod_1.z.string().describe('Token mint address (base58)');
const zPubkey = zod_1.z.string().describe('Solana public key (base58)');
const zAmount = zod_1.z.string().describe('Raw token amount (smallest unit, no decimals)');
const zTx = zod_1.z.string().describe('Base64-encoded serialized transaction');
/* ═══════════════════════════════════════════════════════════════
 *  1. SPL Token — core operations
 * ═══════════════════════════════════════════════════════════════ */
const { register: regSpl, methods: splMethods } = (0, shared_1.createMethodRegistry)('spl-token');
regSpl('deployToken', zod_1.z.object({
    owner: zPubkey.describe('Token owner / mint authority wallet'),
    name: zod_1.z.string().describe('Token name (stored in Metaplex metadata)'),
    symbol: zod_1.z.string().max(10).describe('Token symbol (e.g. "MYTKN")'),
    decimals: zod_1.z.number().int().min(0).max(18).default(9).describe('Token decimals (default: 9)'),
    initialSupply: zAmount.optional().describe('Initial supply to mint to owner (default: 0)'),
    uri: zod_1.z.string().url().optional().describe('URI to off-chain metadata JSON (image, description, etc.)'),
    isMutable: zod_1.z.boolean().optional().default(true).describe('Whether metadata is mutable (default: true)'),
    freezeAuthority: zPubkey.optional().describe('Freeze authority (default: owner). Set to null to disable.'),
    mintAuthority: zPubkey.optional().describe('Mint authority (default: owner). Set to null for fixed supply.'),
    sellerFeeBasisPoints: zod_1.z.number().int().min(0).max(10000).optional().describe('Royalty fee in bps (for fungible with metadata)'),
}), zod_1.z.object({
    mint: zMint.describe('Newly created mint address'),
    ata: zPubkey.describe('Associated token account for the owner'),
    metadataAddress: zPubkey.optional().describe('Metaplex metadata PDA address'),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Deploy a new SPL token with Metaplex metadata. Returns the mint address and an unsigned transaction.');
regSpl('transfer', zod_1.z.object({
    from: zPubkey.describe('Sender wallet address'),
    to: zPubkey.describe('Recipient wallet address'),
    mint: zMint.describe('Token mint address'),
    amount: zAmount.describe('Amount to transfer (raw, smallest unit)'),
    createAta: zod_1.z.boolean().optional().default(true).describe('Create recipient ATA if needed (default: true)'),
    memo: zod_1.z.string().optional().describe('Optional memo to attach to the transaction'),
}), zod_1.z.object({
    tx: zTx,
    fromAta: zPubkey.describe('Sender associated token account'),
    toAta: zPubkey.describe('Recipient associated token account'),
    signature: zod_1.z.string().optional(),
}), 'Transfer SPL tokens between wallets. Optionally creates the recipient ATA.');
regSpl('transferSol', zod_1.z.object({
    from: zPubkey.describe('Sender wallet address'),
    to: zPubkey.describe('Recipient wallet address'),
    amount: zAmount.describe('Amount in lamports (1 SOL = 1_000_000_000 lamports)'),
    memo: zod_1.z.string().optional().describe('Optional memo to attach'),
}), zod_1.z.object({
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Transfer native SOL between wallets via SystemProgram.transfer.');
regSpl('getBalance', zod_1.z.object({
    wallet: zPubkey.describe('Wallet address to check'),
    mint: zMint.optional().describe('Token mint to check balance for (omit for SOL)'),
}), zod_1.z.object({
    balance: zAmount,
    decimals: zod_1.z.number(),
    uiAmount: zod_1.z.number().describe('Human-readable amount with decimals applied'),
    mint: zMint.optional(),
    symbol: zod_1.z.string().optional(),
}), 'Get token or SOL balance for a wallet. If mint is omitted, returns native SOL balance.');
regSpl('getTokenAccounts', zod_1.z.object({
    wallet: zPubkey.describe('Wallet to query all token accounts for'),
    showZeroBalance: zod_1.z.boolean().optional().default(false).describe('Include zero-balance accounts'),
}), zod_1.z.object({
    accounts: zod_1.z.array(zod_1.z.object({
        mint: zMint,
        ata: zPubkey,
        amount: zAmount,
        decimals: zod_1.z.number(),
        uiAmount: zod_1.z.number(),
        symbol: zod_1.z.string().optional(),
        name: zod_1.z.string().optional(),
        logoURI: zod_1.z.string().optional(),
    })),
    totalAccounts: zod_1.z.number(),
    nativeSolBalance: zAmount,
}), 'List all SPL token accounts for a wallet with balances and metadata.');
regSpl('mintTo', zod_1.z.object({
    mint: zMint.describe('Token mint address'),
    mintAuthority: zPubkey.describe('Mint authority wallet'),
    destination: zPubkey.describe('Wallet to receive minted tokens'),
    amount: zAmount.describe('Amount to mint (raw, smallest unit)'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Mint additional tokens to a destination wallet (requires mint authority).');
regSpl('burn', zod_1.z.object({
    mint: zMint,
    owner: zPubkey.describe('Token account owner'),
    amount: zAmount.describe('Amount to burn'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Burn SPL tokens from the owner\'s token account.');
regSpl('freezeAccount', zod_1.z.object({
    mint: zMint,
    account: zPubkey.describe('Token account to freeze'),
    freezeAuthority: zPubkey.describe('Freeze authority wallet'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Freeze a token account (prevents all transfers). Requires freeze authority.');
regSpl('thawAccount', zod_1.z.object({
    mint: zMint,
    account: zPubkey.describe('Token account to thaw'),
    freezeAuthority: zPubkey.describe('Freeze authority wallet'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Thaw (unfreeze) a previously frozen token account.');
regSpl('closeAccount', zod_1.z.object({
    account: zPubkey.describe('Token account to close'),
    owner: zPubkey.describe('Token account owner'),
    destination: zPubkey.optional().describe('Wallet to receive rent SOL (default: owner)'),
}), zod_1.z.object({ tx: zTx, reclaimedLamports: zAmount, signature: zod_1.z.string().optional() }), 'Close a token account and reclaim the rent SOL.');
regSpl('rugCheck', zod_1.z.object({
    mint: zMint.describe('Token mint to analyze'),
}), zod_1.z.object({
    mint: zMint,
    riskLevel: zod_1.z.enum(['safe', 'low', 'medium', 'high', 'critical']),
    risks: zod_1.z.array(zod_1.z.object({
        type: zod_1.z.string(),
        severity: zod_1.z.enum(['info', 'warning', 'critical']),
        message: zod_1.z.string(),
    })),
    details: zod_1.z.object({
        hasMintAuthority: zod_1.z.boolean(),
        hasFreezeAuthority: zod_1.z.boolean(),
        topHoldersPercent: zod_1.z.number().optional(),
        isVerified: zod_1.z.boolean().optional(),
        liquidity: zod_1.z.number().optional(),
        holderCount: zod_1.z.number().optional(),
    }),
}), 'Check a token for potential rug pull risks: mint/freeze authority, holder concentration, liquidity.');
/* ═══════════════════════════════════════════════════════════════
 *  2. Staking — SOL staking + liquid staking protocols
 * ═══════════════════════════════════════════════════════════════ */
const { register: regStake, methods: stakingMethods } = (0, shared_1.createMethodRegistry)('staking');
regStake('stakeSOL', zod_1.z.object({
    wallet: zPubkey.describe('Wallet to stake from'),
    amount: zAmount.describe('Amount in lamports to stake'),
    validator: zPubkey.optional().describe('Validator vote account (default: auto-selected high-quality validator)'),
}), zod_1.z.object({
    stakeAccount: zPubkey,
    tx: zTx,
    estimatedApy: zod_1.z.number().optional(),
    signature: zod_1.z.string().optional(),
}), 'Stake native SOL to a validator. Creates a stake account and delegates.');
regStake('unstakeSOL', zod_1.z.object({
    wallet: zPubkey,
    stakeAccount: zPubkey.describe('Stake account to deactivate and withdraw'),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Deactivate and withdraw SOL from a stake account.');
regStake('getStakeAccounts', zod_1.z.object({
    wallet: zPubkey.describe('Wallet to query stake accounts for'),
}), zod_1.z.object({
    accounts: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        lamports: zAmount,
        state: zod_1.z.enum(['activating', 'active', 'deactivating', 'inactive']),
        validator: zPubkey.optional(),
        activationEpoch: zod_1.z.number().optional(),
        deactivationEpoch: zod_1.z.number().optional(),
    })),
    totalStaked: zAmount,
}), 'List all stake accounts for a wallet with delegation status.');
regStake('stakeJupSOL', zod_1.z.object({
    wallet: zPubkey,
    amount: zAmount.describe('Amount in lamports to stake for jupSOL'),
}), zod_1.z.object({
    tx: zTx,
    jupsolAmount: zAmount.describe('Expected jupSOL received'),
    exchangeRate: zod_1.z.number().describe('SOL/jupSOL exchange rate'),
    signature: zod_1.z.string().optional(),
}), 'Stake SOL for jupSOL (Jupiter liquid staking). Instant liquidity, auto-compounding.');
regStake('unstakeJupSOL', zod_1.z.object({
    wallet: zPubkey,
    amount: zAmount.describe('Amount of jupSOL to unstake'),
}), zod_1.z.object({
    tx: zTx,
    solAmount: zAmount.describe('Expected SOL received'),
    signature: zod_1.z.string().optional(),
}), 'Unstake jupSOL back to SOL.');
regStake('stakeSolayer', zod_1.z.object({
    wallet: zPubkey,
    amount: zAmount.describe('Amount in lamports to stake for sSOL'),
}), zod_1.z.object({
    tx: zTx,
    ssolAmount: zAmount.describe('Expected sSOL received'),
    exchangeRate: zod_1.z.number().describe('SOL/sSOL exchange rate'),
    signature: zod_1.z.string().optional(),
}), 'Stake SOL for sSOL via Solayer. Re-staking protocol with additional yield.');
regStake('unstakeSolayer', zod_1.z.object({
    wallet: zPubkey,
    amount: zAmount.describe('Amount of sSOL to unstake'),
}), zod_1.z.object({
    tx: zTx,
    solAmount: zAmount.describe('Expected SOL received'),
    signature: zod_1.z.string().optional(),
}), 'Unstake sSOL back to SOL via Solayer.');
/* ═══════════════════════════════════════════════════════════════
 *  3. Bridging — Wormhole + deBridge DLN
 * ═══════════════════════════════════════════════════════════════ */
const { register: regBridge, methods: bridgingMethods } = (0, shared_1.createMethodRegistry)('bridging');
regBridge('bridgeWormhole', zod_1.z.object({
    sourceChain: zod_1.z.string().describe('Source chain identifier (e.g. "solana", "ethereum", "base", "polygon")'),
    destinationChain: zod_1.z.string().describe('Destination chain identifier'),
    token: zMint.describe('Token address on source chain'),
    amount: zAmount.describe('Amount to bridge (raw, smallest unit)'),
    sender: zPubkey.describe('Sender wallet on source chain'),
    recipient: zod_1.z.string().describe('Recipient address on destination chain'),
    relayerFee: zAmount.optional().describe('Relayer fee amount (if using automatic relaying)'),
}), zod_1.z.object({
    tx: zTx,
    sequence: zod_1.z.string().describe('Wormhole sequence number'),
    emitterAddress: zod_1.z.string(),
    estimatedArrival: zod_1.z.string().optional().describe('Estimated arrival time (ISO 8601)'),
    signature: zod_1.z.string().optional(),
}), 'Bridge tokens across chains via Wormhole. Supports Solana ↔ EVM chains.');
regBridge('bridgeWormholeStatus', zod_1.z.object({
    sequence: zod_1.z.string().describe('Wormhole sequence number from bridgeWormhole response'),
    sourceChain: zod_1.z.string(),
}), zod_1.z.object({
    status: zod_1.z.enum(['pending', 'signed', 'completed', 'failed']),
    vaaHash: zod_1.z.string().optional(),
    destinationTx: zod_1.z.string().optional(),
    completedAt: zod_1.z.string().optional(),
}), 'Check the status of a Wormhole bridge transfer.');
regBridge('bridgeDeBridge', zod_1.z.object({
    sourceChain: zod_1.z.number().describe('Source chain ID (e.g. 7565164 for Solana)'),
    destinationChain: zod_1.z.number().describe('Destination chain ID'),
    tokenIn: zod_1.z.string().describe('Token address on source chain'),
    tokenOut: zod_1.z.string().describe('Token address on destination chain (use "0x0" for native)'),
    amount: zAmount.describe('Amount to bridge (raw)'),
    sender: zod_1.z.string().describe('Sender address'),
    recipient: zod_1.z.string().describe('Recipient address on destination chain'),
    slippageBps: zod_1.z.number().optional().default(50).describe('Slippage tolerance in bps'),
}), zod_1.z.object({
    tx: zTx,
    orderId: zod_1.z.string().describe('deBridge DLN order ID'),
    estimatedOutput: zAmount,
    fee: zAmount.describe('Bridge fee'),
    estimatedArrival: zod_1.z.string().optional(),
    signature: zod_1.z.string().optional(),
}), 'Bridge tokens across chains via deBridge DLN. Fast cross-chain transfers with deterministic pricing.');
regBridge('bridgeDeBridgeStatus', zod_1.z.object({
    orderId: zod_1.z.string().describe('deBridge DLN order ID'),
}), zod_1.z.object({
    status: zod_1.z.enum(['created', 'fulfilled', 'cancelled', 'expired']),
    sourceChainTx: zod_1.z.string().optional(),
    destinationChainTx: zod_1.z.string().optional(),
    completedAt: zod_1.z.string().optional(),
}), 'Check the status of a deBridge DLN cross-chain transfer.');
/* ═══════════════════════════════════════════════════════════════
 *  Exports
 * ═══════════════════════════════════════════════════════════════ */
exports.tokenSplMethods = splMethods;
exports.tokenStakingMethods = stakingMethods;
exports.tokenBridgingMethods = bridgingMethods;
exports.allTokenMethods = [...splMethods, ...stakingMethods, ...bridgingMethods];
//# sourceMappingURL=schemas.js.map