"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.allDefiMethods = exports.jitoMethods = exports.luloMethods = exports.adrenaMethods = exports.driftMethods = exports.openbookMethods = exports.meteoraMethods = exports.manifestMethods = exports.orcaMethods = exports.raydiumPoolMethods = exports.pumpMethods = void 0;
/**
 * @module ai/plugins/defi/schemas
 * @description DeFi Plugin — Zod schemas for all DeFi protocol integrations.
 *
 * Covers:
 *  - PumpPortal        (2 methods — launch token, buy/sell on Pump.fun)
 *  - Raydium Pools     (5 methods — CPMM, CLMM, AMMv4 pool creation + liquidity)
 *  - Orca Whirlpool    (5 methods — pool info, swap, positions, liquidity)
 *  - Manifest          (4 methods — market creation, limit orders, cancel, orderbook)
 *  - Meteora           (5 methods — Dynamic AMM, DLMM, Alpha Vault)
 *  - Openbook          (3 methods — market creation, place order, cancel)
 *  - Drift             (7 methods — vaults, perps, lending, borrowing)
 *  - Adrena            (5 methods — perpetuals trading)
 *  - Lulo              (4 methods — lending/borrowing with best APR)
 *  - Jito              (3 methods — bundles, tips)
 *
 * @since 2.0.0
 */
const zod_1 = require("zod");
const shared_1 = require("../../tools/protocols/shared");
const zPubkey = zod_1.z.string().describe('Solana public key (base58)');
const zMint = zod_1.z.string().describe('Token mint address (base58)');
const zAmount = zod_1.z.string().describe('Raw token amount (smallest unit)');
const zTx = zod_1.z.string().describe('Base64-encoded serialized transaction');
/* ═══════════════════════════════════════════════════════════════
 *  1. PumpPortal — Launch on Pump.fun
 * ═══════════════════════════════════════════════════════════════ */
const { register: regPump, methods: pumpMethods } = (0, shared_1.createMethodRegistry)('pump');
exports.pumpMethods = pumpMethods;
regPump('launchToken', zod_1.z.object({
    deployer: zPubkey.describe('Deployer wallet'),
    name: zod_1.z.string().describe('Token name'),
    symbol: zod_1.z.string().max(10).describe('Token ticker'),
    description: zod_1.z.string().optional(),
    image: zod_1.z.string().url().optional().describe('Token logo URL'),
    twitter: zod_1.z.string().optional().describe('Twitter/X handle'),
    telegram: zod_1.z.string().optional().describe('Telegram group link'),
    website: zod_1.z.string().url().optional().describe('Website URL'),
    initialBuyAmount: zAmount.optional().describe('SOL amount to buy at launch (dev buy)'),
}), zod_1.z.object({
    mint: zMint,
    bondingCurve: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Launch a new token on Pump.fun via PumpPortal. Optionally include an initial dev buy.');
regPump('trade', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint.describe('Pump.fun token mint'),
    action: zod_1.z.enum(['buy', 'sell']).describe('Trade direction'),
    amount: zAmount.describe('Amount in SOL (buy) or tokens (sell)'),
    slippageBps: zod_1.z.number().min(0).max(10000).optional().default(500),
}), zod_1.z.object({
    tx: zTx,
    expectedOutput: zAmount,
    priceImpact: zod_1.z.string().optional(),
    signature: zod_1.z.string().optional(),
}), 'Buy or sell tokens on Pump.fun bonding curve.');
/* ═══════════════════════════════════════════════════════════════
 *  2. Raydium — Pool Creation + Liquidity
 * ═══════════════════════════════════════════════════════════════ */
const { register: regRayPool, methods: raydiumPoolMethods } = (0, shared_1.createMethodRegistry)('raydium-pools');
exports.raydiumPoolMethods = raydiumPoolMethods;
regRayPool('createCPMM', zod_1.z.object({
    creator: zPubkey,
    mintA: zMint.describe('Token A mint'),
    mintB: zMint.describe('Token B mint'),
    amountA: zAmount.describe('Initial liquidity for token A'),
    amountB: zAmount.describe('Initial liquidity for token B'),
    startTime: zod_1.z.number().optional().describe('Pool start time (Unix timestamp)'),
}), zod_1.z.object({
    poolId: zPubkey,
    lpMint: zMint,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a Raydium CPMM (Constant Product Market Maker) pool.');
regRayPool('createCLMM', zod_1.z.object({
    creator: zPubkey,
    mintA: zMint,
    mintB: zMint,
    initialPrice: zod_1.z.number().positive().describe('Initial price of token A in terms of token B'),
    tickSpacing: zod_1.z.number().int().positive().describe('Tick spacing (defines fee tier: 1=0.01%, 10=0.05%, 60=0.3%, 200=1%)'),
    amountA: zAmount.describe('Initial liquidity for token A'),
    amountB: zAmount.describe('Initial liquidity for token B'),
    priceLower: zod_1.z.number().positive().describe('Lower price bound for liquidity range'),
    priceUpper: zod_1.z.number().positive().describe('Upper price bound for liquidity range'),
    startTime: zod_1.z.number().optional(),
}), zod_1.z.object({
    poolId: zPubkey,
    positionNft: zMint.optional(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a Raydium CLMM (Concentrated Liquidity) pool with a price range.');
regRayPool('createAMMv4', zod_1.z.object({
    creator: zPubkey,
    marketId: zPubkey.describe('Openbook market ID'),
    baseAmount: zAmount,
    quoteAmount: zAmount,
    startTime: zod_1.z.number().optional(),
}), zod_1.z.object({
    ammId: zPubkey,
    lpMint: zMint,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a Raydium AMM v4 pool (requires an existing Openbook market).');
regRayPool('addLiquidity', zod_1.z.object({
    wallet: zPubkey,
    poolId: zPubkey.describe('Raydium pool ID'),
    amountA: zAmount.optional().describe('Token A amount (for CPMM/AMM)'),
    amountB: zAmount.optional().describe('Token B amount (for CPMM/AMM)'),
    priceLower: zod_1.z.number().optional().describe('Lower price for CLMM range'),
    priceUpper: zod_1.z.number().optional().describe('Upper price for CLMM range'),
    fixedSide: zod_1.z.enum(['a', 'b']).optional().describe('Fixed side for single-sided liquidity'),
}), zod_1.z.object({
    tx: zTx,
    lpAmount: zAmount.optional(),
    positionNft: zMint.optional(),
    signature: zod_1.z.string().optional(),
}), 'Add liquidity to a Raydium pool (CPMM, CLMM, or AMM v4).');
regRayPool('removeLiquidity', zod_1.z.object({
    wallet: zPubkey,
    poolId: zPubkey,
    lpAmount: zAmount.optional().describe('LP tokens to burn (for CPMM/AMM)'),
    positionNft: zMint.optional().describe('Position NFT (for CLMM)'),
    percentage: zod_1.z.number().min(0).max(100).optional().describe('Percentage of liquidity to remove'),
}), zod_1.z.object({
    tx: zTx,
    amountA: zAmount,
    amountB: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Remove liquidity from a Raydium pool.');
/* ═══════════════════════════════════════════════════════════════
 *  3. Orca Whirlpool
 * ═══════════════════════════════════════════════════════════════ */
const { register: regOrca, methods: orcaMethods } = (0, shared_1.createMethodRegistry)('orca');
exports.orcaMethods = orcaMethods;
regOrca('getWhirlpool', zod_1.z.object({
    poolAddress: zPubkey.describe('Whirlpool pool address'),
}), zod_1.z.object({
    address: zPubkey,
    tokenMintA: zMint,
    tokenMintB: zMint,
    tickSpacing: zod_1.z.number(),
    sqrtPrice: zod_1.z.string(),
    liquidity: zod_1.z.string(),
    feeRate: zod_1.z.number(),
    protocolFeeRate: zod_1.z.number(),
    currentTickIndex: zod_1.z.number(),
}), 'Get Orca Whirlpool pool information.');
regOrca('swap', zod_1.z.object({
    wallet: zPubkey,
    poolAddress: zPubkey,
    inputMint: zMint,
    amount: zAmount,
    slippageBps: zod_1.z.number().min(0).max(10000).optional().default(100),
    isExactInput: zod_1.z.boolean().optional().default(true),
}), zod_1.z.object({
    tx: zTx,
    estimatedOutput: zAmount,
    priceImpact: zod_1.z.string(),
    fee: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Swap tokens on an Orca Whirlpool.');
regOrca('openPosition', zod_1.z.object({
    wallet: zPubkey,
    poolAddress: zPubkey,
    priceLower: zod_1.z.number().positive(),
    priceUpper: zod_1.z.number().positive(),
    amountA: zAmount.optional(),
    amountB: zAmount.optional(),
}), zod_1.z.object({
    positionMint: zMint,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Open a concentrated liquidity position on an Orca Whirlpool.');
regOrca('closePosition', zod_1.z.object({
    wallet: zPubkey,
    positionMint: zMint,
}), zod_1.z.object({
    tx: zTx,
    collectedA: zAmount,
    collectedB: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Close a Whirlpool position and collect all fees/rewards.');
regOrca('collectFees', zod_1.z.object({
    wallet: zPubkey,
    positionMint: zMint,
}), zod_1.z.object({
    tx: zTx,
    feesA: zAmount,
    feesB: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Collect accumulated fees from a Whirlpool position.');
/* ═══════════════════════════════════════════════════════════════
 *  4. Manifest — Market Creation + Limit Orders
 * ═══════════════════════════════════════════════════════════════ */
const { register: regManifest, methods: manifestMethods } = (0, shared_1.createMethodRegistry)('manifest');
exports.manifestMethods = manifestMethods;
regManifest('createMarket', zod_1.z.object({
    creator: zPubkey,
    baseMint: zMint.describe('Base token mint'),
    quoteMint: zMint.describe('Quote token mint'),
}), zod_1.z.object({
    marketId: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a new Manifest market for a token pair.');
regManifest('placeLimitOrder', zod_1.z.object({
    wallet: zPubkey,
    marketId: zPubkey,
    side: zod_1.z.enum(['buy', 'sell']),
    price: zod_1.z.number().positive().describe('Limit price'),
    size: zAmount.describe('Order size in base tokens'),
    orderType: zod_1.z.enum(['limit', 'postOnly', 'ioc']).optional().default('limit'),
}), zod_1.z.object({
    orderId: zod_1.z.string(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Place a limit order on a Manifest market.');
regManifest('cancelOrder', zod_1.z.object({
    wallet: zPubkey,
    marketId: zPubkey,
    orderId: zod_1.z.string(),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Cancel a limit order on a Manifest market.');
regManifest('getOrderbook', zod_1.z.object({
    marketId: zPubkey,
    depth: zod_1.z.number().int().min(1).max(100).optional().default(20),
}), zod_1.z.object({
    bids: zod_1.z.array(zod_1.z.object({ price: zod_1.z.number(), size: zod_1.z.string() })),
    asks: zod_1.z.array(zod_1.z.object({ price: zod_1.z.number(), size: zod_1.z.string() })),
    lastTradePrice: zod_1.z.number().optional(),
    spread: zod_1.z.number().optional(),
}), 'Get the current orderbook for a Manifest market.');
/* ═══════════════════════════════════════════════════════════════
 *  5. Meteora — Dynamic AMM, DLMM, Alpha Vault
 * ═══════════════════════════════════════════════════════════════ */
const { register: regMeteora, methods: meteoraMethods } = (0, shared_1.createMethodRegistry)('meteora');
exports.meteoraMethods = meteoraMethods;
regMeteora('createDynamicPool', zod_1.z.object({
    creator: zPubkey,
    mintA: zMint,
    mintB: zMint,
    amountA: zAmount,
    amountB: zAmount,
    tradeFeeNumerator: zod_1.z.number().int().optional().describe('Trade fee numerator (default pool fee)'),
    tradeFeeDenominator: zod_1.z.number().int().optional(),
    activationType: zod_1.z.enum(['slot', 'timestamp']).optional(),
    activationPoint: zod_1.z.number().optional().describe('Activation slot/timestamp'),
}), zod_1.z.object({
    poolAddress: zPubkey,
    lpMint: zMint,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a Meteora Dynamic AMM pool.');
regMeteora('createDLMMPool', zod_1.z.object({
    creator: zPubkey,
    mintX: zMint.describe('Token X mint'),
    mintY: zMint.describe('Token Y mint'),
    binStep: zod_1.z.number().int().positive().describe('Bin step size (defines fee tier and price granularity)'),
    initialPrice: zod_1.z.number().positive(),
    amountX: zAmount.optional(),
    amountY: zAmount.optional(),
    priceRoundingUp: zod_1.z.boolean().optional().default(true),
    feeBps: zod_1.z.number().int().min(0).max(10000).optional(),
    activationType: zod_1.z.enum(['slot', 'timestamp']).optional(),
    activationPoint: zod_1.z.number().optional(),
}), zod_1.z.object({
    poolAddress: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a Meteora DLMM (Discrete Liquidity Market Maker) pool.');
regMeteora('addDLMMLiquidity', zod_1.z.object({
    wallet: zPubkey,
    poolAddress: zPubkey,
    amountX: zAmount,
    amountY: zAmount,
    strategy: zod_1.z.enum(['spot', 'curve', 'bid-ask']).optional().default('spot')
        .describe('Liquidity distribution strategy'),
    minBinId: zod_1.z.number().int().optional(),
    maxBinId: zod_1.z.number().int().optional(),
}), zod_1.z.object({
    positionAddress: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Add liquidity to a Meteora DLMM pool with a distribution strategy.');
regMeteora('removeDLMMLiquidity', zod_1.z.object({
    wallet: zPubkey,
    positionAddress: zPubkey,
    percentage: zod_1.z.number().min(0).max(100).default(100),
}), zod_1.z.object({
    tx: zTx,
    amountX: zAmount,
    amountY: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Remove liquidity from a Meteora DLMM position.');
regMeteora('createAlphaVault', zod_1.z.object({
    creator: zPubkey,
    poolAddress: zPubkey,
    depositMint: zMint.describe('Token mint accepted for deposits'),
    maxDeposit: zAmount.describe('Maximum deposit cap'),
    vestingDuration: zod_1.z.number().int().positive().describe('Vesting duration in seconds'),
    startTime: zod_1.z.number().optional().describe('Vault start time (Unix timestamp)'),
}), zod_1.z.object({
    vaultAddress: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a Meteora Alpha Vault for fair token launches with vesting.');
/* ═══════════════════════════════════════════════════════════════
 *  6. Openbook — Market Creation
 * ═══════════════════════════════════════════════════════════════ */
const { register: regOb, methods: openbookMethods } = (0, shared_1.createMethodRegistry)('openbook');
exports.openbookMethods = openbookMethods;
regOb('createMarket', zod_1.z.object({
    creator: zPubkey,
    baseMint: zMint,
    quoteMint: zMint,
    lotSize: zod_1.z.number().positive().describe('Minimum order size'),
    tickSize: zod_1.z.number().positive().describe('Minimum price increment'),
}), zod_1.z.object({
    marketId: zPubkey,
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Create a new Openbook DEX market for a token pair.');
regOb('placeOrder', zod_1.z.object({
    wallet: zPubkey,
    marketId: zPubkey,
    side: zod_1.z.enum(['buy', 'sell']),
    price: zod_1.z.number().positive(),
    size: zAmount,
    orderType: zod_1.z.enum(['limit', 'ioc', 'postOnly']).optional().default('limit'),
}), zod_1.z.object({
    orderId: zod_1.z.string(),
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Place an order on an Openbook market.');
regOb('cancelOrder', zod_1.z.object({
    wallet: zPubkey,
    marketId: zPubkey,
    orderId: zod_1.z.string(),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Cancel an open order on an Openbook market.');
/* ═══════════════════════════════════════════════════════════════
 *  7. Drift — Vaults, Perps, Lending, Borrowing
 * ═══════════════════════════════════════════════════════════════ */
const { register: regDrift, methods: driftMethods } = (0, shared_1.createMethodRegistry)('drift');
exports.driftMethods = driftMethods;
regDrift('deposit', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint.describe('Token to deposit (e.g. USDC, SOL)'),
    amount: zAmount,
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Deposit tokens into a Drift account for trading or lending.');
regDrift('withdraw', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint,
    amount: zAmount,
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Withdraw tokens from a Drift account.');
regDrift('openPerpPosition', zod_1.z.object({
    wallet: zPubkey,
    marketIndex: zod_1.z.number().int().describe('Drift perp market index (0=SOL-PERP, 1=BTC-PERP, etc.)'),
    direction: zod_1.z.enum(['long', 'short']),
    size: zAmount.describe('Position size in base token units'),
    leverage: zod_1.z.number().positive().max(20).optional().describe('Leverage multiplier (max 20x)'),
    orderType: zod_1.z.enum(['market', 'limit']).optional().default('market'),
    price: zod_1.z.number().positive().optional().describe('Limit price (required for limit orders)'),
    reduceOnly: zod_1.z.boolean().optional().default(false),
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({
    tx: zTx,
    orderId: zod_1.z.number().optional(),
    estimatedEntryPrice: zod_1.z.number().optional(),
    signature: zod_1.z.string().optional(),
}), 'Open a perpetual futures position on Drift.');
regDrift('closePerpPosition', zod_1.z.object({
    wallet: zPubkey,
    marketIndex: zod_1.z.number().int(),
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({
    tx: zTx,
    pnl: zod_1.z.string().optional().describe('Realized PnL'),
    signature: zod_1.z.string().optional(),
}), 'Close a perpetual position on Drift.');
regDrift('getPositions', zod_1.z.object({
    wallet: zPubkey,
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({
    perpPositions: zod_1.z.array(zod_1.z.object({
        marketIndex: zod_1.z.number(),
        baseAssetAmount: zod_1.z.string(),
        quoteAssetAmount: zod_1.z.string(),
        direction: zod_1.z.enum(['long', 'short']),
        unrealizedPnl: zod_1.z.string(),
        entryPrice: zod_1.z.number(),
        markPrice: zod_1.z.number(),
        leverage: zod_1.z.number().optional(),
    })),
    spotPositions: zod_1.z.array(zod_1.z.object({
        marketIndex: zod_1.z.number(),
        balance: zod_1.z.string(),
        tokenSymbol: zod_1.z.string().optional(),
    })),
    accountEquity: zod_1.z.string(),
    freeCollateral: zod_1.z.string(),
}), 'Get all open positions and account info for a Drift account.');
regDrift('lend', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint,
    amount: zAmount,
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({
    tx: zTx,
    estimatedApy: zod_1.z.number().optional(),
    signature: zod_1.z.string().optional(),
}), 'Lend tokens on Drift to earn interest.');
regDrift('borrow', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint,
    amount: zAmount,
    subAccountId: zod_1.z.number().int().optional().default(0),
}), zod_1.z.object({
    tx: zTx,
    borrowRate: zod_1.z.number().optional(),
    signature: zod_1.z.string().optional(),
}), 'Borrow tokens on Drift against deposited collateral.');
/* ═══════════════════════════════════════════════════════════════
 *  8. Adrena — Perpetuals Trading
 * ═══════════════════════════════════════════════════════════════ */
const { register: regAdrena, methods: adrenaMethods } = (0, shared_1.createMethodRegistry)('adrena');
exports.adrenaMethods = adrenaMethods;
regAdrena('openPosition', zod_1.z.object({
    wallet: zPubkey,
    market: zod_1.z.string().describe('Market symbol (e.g. "SOL", "BTC", "ETH")'),
    collateralMint: zMint.describe('Collateral token mint (e.g. USDC, SOL)'),
    collateralAmount: zAmount,
    side: zod_1.z.enum(['long', 'short']),
    leverage: zod_1.z.number().positive().max(100).describe('Leverage multiplier'),
    stopLoss: zod_1.z.number().optional().describe('Stop loss price'),
    takeProfit: zod_1.z.number().optional().describe('Take profit price'),
}), zod_1.z.object({
    positionKey: zPubkey,
    tx: zTx,
    estimatedEntryPrice: zod_1.z.number(),
    estimatedSize: zAmount,
    signature: zod_1.z.string().optional(),
}), 'Open a leveraged perpetual position on Adrena Protocol.');
regAdrena('closePosition', zod_1.z.object({
    wallet: zPubkey,
    positionKey: zPubkey,
}), zod_1.z.object({
    tx: zTx,
    pnl: zod_1.z.string(),
    signature: zod_1.z.string().optional(),
}), 'Close a perpetual position on Adrena Protocol.');
regAdrena('addCollateral', zod_1.z.object({
    wallet: zPubkey,
    positionKey: zPubkey,
    amount: zAmount,
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Add collateral to an existing Adrena position.');
regAdrena('removeCollateral', zod_1.z.object({
    wallet: zPubkey,
    positionKey: zPubkey,
    amount: zAmount,
}), zod_1.z.object({ tx: zTx, signature: zod_1.z.string().optional() }), 'Remove collateral from an Adrena position.');
regAdrena('getPositions', zod_1.z.object({
    wallet: zPubkey,
}), zod_1.z.object({
    positions: zod_1.z.array(zod_1.z.object({
        positionKey: zPubkey,
        market: zod_1.z.string(),
        side: zod_1.z.enum(['long', 'short']),
        size: zAmount,
        collateral: zAmount,
        entryPrice: zod_1.z.number(),
        markPrice: zod_1.z.number(),
        leverage: zod_1.z.number(),
        unrealizedPnl: zod_1.z.string(),
        liquidationPrice: zod_1.z.number(),
    })),
}), 'Get all open positions on Adrena Protocol.');
/* ═══════════════════════════════════════════════════════════════
 *  9. Lulo — Best APR Lending
 * ═══════════════════════════════════════════════════════════════ */
const { register: regLulo, methods: luloMethods } = (0, shared_1.createMethodRegistry)('lulo');
exports.luloMethods = luloMethods;
regLulo('deposit', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint.describe('Token to deposit (e.g. USDC)'),
    amount: zAmount,
}), zod_1.z.object({
    tx: zTx,
    protocol: zod_1.z.string().describe('Selected lending protocol (best APR)'),
    estimatedApy: zod_1.z.number(),
    signature: zod_1.z.string().optional(),
}), 'Deposit into the best APR lending protocol via Lulo aggregator.');
regLulo('withdraw', zod_1.z.object({
    wallet: zPubkey,
    mint: zMint,
    amount: zAmount,
}), zod_1.z.object({
    tx: zTx,
    signature: zod_1.z.string().optional(),
}), 'Withdraw from Lulo lending position.');
regLulo('getBestRates', zod_1.z.object({
    mint: zMint.optional().describe('Token to check rates for (default: USDC)'),
}), zod_1.z.object({
    rates: zod_1.z.array(zod_1.z.object({
        protocol: zod_1.z.string(),
        apy: zod_1.z.number(),
        tvl: zod_1.z.string().optional(),
        utilizationRate: zod_1.z.number().optional(),
    })),
    bestRate: zod_1.z.object({
        protocol: zod_1.z.string(),
        apy: zod_1.z.number(),
    }),
}), 'Get the best lending/yield rates across all protocols via Lulo.');
regLulo('getPositions', zod_1.z.object({
    wallet: zPubkey,
}), zod_1.z.object({
    positions: zod_1.z.array(zod_1.z.object({
        protocol: zod_1.z.string(),
        mint: zMint,
        deposited: zAmount,
        earned: zAmount,
        currentApy: zod_1.z.number(),
    })),
    totalValueUsd: zod_1.z.number().optional(),
}), 'Get all Lulo lending positions for a wallet.');
/* ═══════════════════════════════════════════════════════════════
 *  10. Jito — Bundles + Tips
 * ═══════════════════════════════════════════════════════════════ */
const { register: regJito, methods: jitoMethods } = (0, shared_1.createMethodRegistry)('jito');
exports.jitoMethods = jitoMethods;
regJito('sendBundle', zod_1.z.object({
    transactions: zod_1.z.array(zTx).min(1).max(5)
        .describe('Array of base64-encoded signed transactions (max 5)'),
    tipLamports: zAmount.optional().describe('Jito tip amount in lamports (higher = faster inclusion)'),
}), zod_1.z.object({
    bundleId: zod_1.z.string(),
    status: zod_1.z.enum(['submitted', 'landed', 'failed']),
    slot: zod_1.z.number().optional(),
    signatures: zod_1.z.array(zod_1.z.string()),
}), 'Send a Jito bundle for MEV-protected, atomic transaction execution.');
regJito('getBundleStatus', zod_1.z.object({
    bundleId: zod_1.z.string(),
}), zod_1.z.object({
    status: zod_1.z.enum(['pending', 'landed', 'failed', 'dropped']),
    slot: zod_1.z.number().optional(),
    confirmationStatus: zod_1.z.string().optional(),
    error: zod_1.z.string().optional(),
}), 'Check the status of a submitted Jito bundle.');
regJito('getTipEstimate', zod_1.z.object({}), zod_1.z.object({
    percentile25: zAmount.describe('25th percentile tip (lamports)'),
    percentile50: zAmount.describe('50th percentile tip'),
    percentile75: zAmount.describe('75th percentile tip'),
    percentile95: zAmount.describe('95th percentile tip'),
    percentile99: zAmount.describe('99th percentile tip'),
}), 'Get current Jito tip percentile estimates for bundle inclusion priority.');
exports.allDefiMethods = [
    ...pumpMethods,
    ...raydiumPoolMethods,
    ...orcaMethods,
    ...manifestMethods,
    ...meteoraMethods,
    ...openbookMethods,
    ...driftMethods,
    ...adrenaMethods,
    ...luloMethods,
    ...jitoMethods,
];
//# sourceMappingURL=schemas.js.map