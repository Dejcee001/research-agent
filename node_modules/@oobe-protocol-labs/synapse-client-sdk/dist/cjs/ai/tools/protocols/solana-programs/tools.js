"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.solanaProgramsMethodNames = exports.solanaProgramsMethods = void 0;
exports.createSolanaProgramsTools = createSolanaProgramsTools;
/**
 * @module ai/tools/protocols/solana-programs/tools
 * @description Solana Native Programs — LangChain tool factory.
 *
 * Creates 16 instruction-building tools that encode Solana native
 * program instructions (System, SPL Token, ATA, Compute Budget, Memo).
 * These tools run locally — no RPC or REST calls needed.
 *
 * ```ts
 * const programs = createSolanaProgramsTools();
 * agent.tools.push(...programs.tools);
 *
 * // Agent can now build instructions:
 * // "Transfer 1 SOL from <wallet> to <recipient>"
 * // → calls solana_programs_systemTransfer tool
 * ```
 *
 * @since 1.1.0
 */
const shared_1 = require("../shared");
const schemas_1 = require("./schemas");
const types_1 = require("../../../../core/types");
const system_1 = require("../../../../programs/system");
const spl_token_1 = require("../../../../programs/spl-token");
const associated_token_1 = require("../../../../programs/associated-token");
const compute_budget_1 = require("../../../../programs/compute-budget");
const memo_1 = require("../../../../programs/memo");
/* ═══════════════════════════════════════════════════════════════
 *  Serialiser — TransactionInstruction → JSON-friendly output
 * ═══════════════════════════════════════════════════════════════ */
/** @internal Convert Uint8Array to base64 for JSON transport */
function uint8ToBase64(data) {
    // Node.js path (fastest)
    if (typeof Buffer !== 'undefined')
        return Buffer.from(data).toString('base64');
    // Browser path
    let binary = '';
    for (let i = 0; i < data.length; i++)
        binary += String.fromCharCode(data[i]);
    return btoa(binary);
}
/** @internal Serialise a TransactionInstruction for LLM consumption */
function serialiseInstruction(ix) {
    return {
        programId: ix.programId,
        keys: ix.keys.map((k) => ({
            pubkey: k.pubkey,
            isSigner: k.isSigner,
            isWritable: k.isWritable,
        })),
        data: uint8ToBase64(ix.data),
    };
}
/* ═══════════════════════════════════════════════════════════════
 *  Execution dispatcher — local instruction encoding
 * ═══════════════════════════════════════════════════════════════ */
function createProgramsExecutor() {
    return async (method, input) => {
        switch (method.name) {
            // ── System Program ───────────────────────────────────────
            case 'systemTransfer':
                return serialiseInstruction(system_1.SystemProgram.transfer({
                    from: (0, types_1.Pubkey)(input.from),
                    to: (0, types_1.Pubkey)(input.to),
                    lamports: BigInt(input.lamports),
                }));
            case 'systemCreateAccount':
                return serialiseInstruction(system_1.SystemProgram.createAccount({
                    from: (0, types_1.Pubkey)(input.from),
                    newAccount: (0, types_1.Pubkey)(input.newAccount),
                    lamports: BigInt(input.lamports),
                    space: input.space,
                    programId: (0, types_1.Pubkey)(input.programId),
                }));
            case 'systemAllocate':
                return serialiseInstruction(system_1.SystemProgram.allocate({
                    account: (0, types_1.Pubkey)(input.account),
                    space: input.space,
                }));
            case 'systemAssign':
                return serialiseInstruction(system_1.SystemProgram.assign({
                    account: (0, types_1.Pubkey)(input.account),
                    programId: (0, types_1.Pubkey)(input.programId),
                }));
            // ── SPL Token ────────────────────────────────────────────
            case 'tokenTransfer':
                return serialiseInstruction(spl_token_1.SplToken.transfer({
                    source: (0, types_1.Pubkey)(input.source),
                    destination: (0, types_1.Pubkey)(input.destination),
                    owner: (0, types_1.Pubkey)(input.owner),
                    amount: BigInt(input.amount),
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            case 'tokenTransferChecked':
                return serialiseInstruction(spl_token_1.SplToken.transferChecked({
                    source: (0, types_1.Pubkey)(input.source),
                    mint: (0, types_1.Pubkey)(input.mint),
                    destination: (0, types_1.Pubkey)(input.destination),
                    owner: (0, types_1.Pubkey)(input.owner),
                    amount: BigInt(input.amount),
                    decimals: input.decimals,
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            case 'tokenApprove':
                return serialiseInstruction(spl_token_1.SplToken.approve({
                    account: (0, types_1.Pubkey)(input.account),
                    delegate: (0, types_1.Pubkey)(input.delegate),
                    owner: (0, types_1.Pubkey)(input.owner),
                    amount: BigInt(input.amount),
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            case 'tokenRevoke':
                return serialiseInstruction(spl_token_1.SplToken.revoke({
                    account: (0, types_1.Pubkey)(input.account),
                    owner: (0, types_1.Pubkey)(input.owner),
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            case 'tokenMintTo':
                return serialiseInstruction(spl_token_1.SplToken.mintTo({
                    mint: (0, types_1.Pubkey)(input.mint),
                    destination: (0, types_1.Pubkey)(input.destination),
                    authority: (0, types_1.Pubkey)(input.authority),
                    amount: BigInt(input.amount),
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            case 'tokenBurn':
                return serialiseInstruction(spl_token_1.SplToken.burn({
                    account: (0, types_1.Pubkey)(input.account),
                    mint: (0, types_1.Pubkey)(input.mint),
                    owner: (0, types_1.Pubkey)(input.owner),
                    amount: BigInt(input.amount),
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            case 'tokenCloseAccount':
                return serialiseInstruction(spl_token_1.SplToken.closeAccount({
                    account: (0, types_1.Pubkey)(input.account),
                    destination: (0, types_1.Pubkey)(input.destination),
                    owner: (0, types_1.Pubkey)(input.owner),
                    programId: input.programId ? (0, types_1.Pubkey)(input.programId) : undefined,
                }));
            // ── Associated Token Account ─────────────────────────────
            case 'ataCreate':
                return serialiseInstruction(associated_token_1.AssociatedToken.create({
                    payer: (0, types_1.Pubkey)(input.payer),
                    associatedToken: (0, types_1.Pubkey)(input.associatedToken),
                    owner: (0, types_1.Pubkey)(input.owner),
                    mint: (0, types_1.Pubkey)(input.mint),
                    tokenProgramId: input.tokenProgramId ? (0, types_1.Pubkey)(input.tokenProgramId) : undefined,
                }));
            case 'ataCreateIdempotent':
                return serialiseInstruction(associated_token_1.AssociatedToken.createIdempotent({
                    payer: (0, types_1.Pubkey)(input.payer),
                    associatedToken: (0, types_1.Pubkey)(input.associatedToken),
                    owner: (0, types_1.Pubkey)(input.owner),
                    mint: (0, types_1.Pubkey)(input.mint),
                    tokenProgramId: input.tokenProgramId ? (0, types_1.Pubkey)(input.tokenProgramId) : undefined,
                }));
            // ── Compute Budget ───────────────────────────────────────
            case 'setComputeUnitLimit':
                return serialiseInstruction(compute_budget_1.ComputeBudget.setComputeUnitLimit({
                    units: input.units,
                }));
            case 'setComputeUnitPrice':
                return serialiseInstruction(compute_budget_1.ComputeBudget.setComputeUnitPrice({
                    microLamports: BigInt(input.microLamports),
                }));
            // ── Memo ─────────────────────────────────────────────────
            case 'addMemo':
                return serialiseInstruction(memo_1.Memo.addMemo({
                    message: input.message,
                    signer: input.signer ? (0, types_1.Pubkey)(input.signer) : undefined,
                }));
            default:
                throw new Error(`Unknown Solana program method: ${method.name}`);
        }
    };
}
/* ═══════════════════════════════════════════════════════════════
 *  createSolanaProgramsTools()
 *
 *  Public factory — the single entry-point for consumers.
 * ═══════════════════════════════════════════════════════════════ */
/**
 * @description Create LangChain-compatible tools for Solana native programs.
 *
 * All 16 tools run locally — they encode instructions using the SDK's
 * native binary encoders. No RPC or REST API calls are made.
 *
 * @param {SolanaProgramsToolsConfig & CreateProtocolToolsOpts} [config={}] - Tool options
 * @returns {ProtocolToolkit} Toolkit with 16 instruction-building tools
 *
 * @example
 * ```ts
 * const programs = createSolanaProgramsTools();
 * const agent = createAgent({ tools: programs.tools });
 *
 * // Agent builds a SOL transfer instruction:
 * const result = await programs.toolMap.systemTransfer.invoke({
 *   from: 'SenderWallet...',
 *   to: 'ReceiverWallet...',
 *   lamports: '1000000000',
 * });
 *
 * // Cherry-pick:
 * const transferTool = programs.toolMap.tokenTransferChecked;
 * ```
 *
 * @since 1.1.0
 */
function createSolanaProgramsTools(config = {}) {
    const { ...toolOpts } = config;
    const execute = createProgramsExecutor();
    return (0, shared_1.buildProtocolTools)(schemas_1.solanaProgramsMethods, execute, {
        defaultPrefix: 'solana_programs_',
        ...toolOpts,
    });
}
/** Re-export schemas for direct access. */
var schemas_2 = require("./schemas");
Object.defineProperty(exports, "solanaProgramsMethods", { enumerable: true, get: function () { return schemas_2.solanaProgramsMethods; } });
Object.defineProperty(exports, "solanaProgramsMethodNames", { enumerable: true, get: function () { return schemas_2.solanaProgramsMethodNames; } });
//# sourceMappingURL=tools.js.map