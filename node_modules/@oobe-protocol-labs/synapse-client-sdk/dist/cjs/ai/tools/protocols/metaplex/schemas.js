"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.metaplexMethodNames = exports.metaplexMethods = void 0;
/**
 * @module ai/tools/protocols/metaplex/schemas
 * @description Metaplex Protocol — Zod schemas for 12 DAS + NFT helper methods.
 *
 * Covers:
 *  - DAS API     (11 methods — wraps existing src/das/ via RPC)
 *  - Helpers      (1 method  — collection resolver)
 *
 * These methods go through the Solana JSON-RPC transport (DAS extension),
 * NOT a separate REST API like Jupiter/Raydium.
 *
 * @since 1.0.0
 */
const zod_1 = require("zod");
const shared_1 = require("../shared");
const { register, methods } = (0, shared_1.createMethodRegistry)('metaplex');
/* ═══════════════════════════════════════════════════════════════
 *  Shared Zod primitives
 * ═══════════════════════════════════════════════════════════════ */
const zPubkey = zod_1.z.string().describe('Base58-encoded Solana public key');
const zCommitment = zod_1.z.enum(['processed', 'confirmed', 'finalized']).optional();
const zSortBy = zod_1.z.object({
    sortBy: zod_1.z.enum(['created', 'updated', 'recent_action', 'none']).describe('Sort field'),
    sortDirection: zod_1.z.enum(['asc', 'desc']).describe('Sort direction'),
}).optional().describe('Sorting options');
const zPagination = {
    page: zod_1.z.number().int().positive().optional().describe('Page number (1-indexed)'),
    limit: zod_1.z.number().int().positive().max(1000).optional().describe('Items per page (max 1000)'),
    cursor: zod_1.z.string().optional().describe('Cursor for keyset pagination'),
    before: zod_1.z.string().optional().describe('Cursor: return items before this'),
    after: zod_1.z.string().optional().describe('Cursor: return items after this'),
};
const zDisplayOpts = {
    showFungible: zod_1.z.boolean().optional().describe('Include fungible tokens'),
    showNativeBalance: zod_1.z.boolean().optional().describe('Include SOL balance'),
    showInscription: zod_1.z.boolean().optional().describe('Include inscription data'),
    showCollectionMetadata: zod_1.z.boolean().optional().describe('Include collection metadata'),
    showZeroBalance: zod_1.z.boolean().optional().describe('Include zero-balance accounts'),
    showUnverifiedCollections: zod_1.z.boolean().optional().describe('Include unverified collections'),
};
/* ── Output shapes ──────────────────────────────────────────── */
const zDasContent = zod_1.z.object({
    json_uri: zod_1.z.string().optional(),
    metadata: zod_1.z.object({
        name: zod_1.z.string().optional(),
        symbol: zod_1.z.string().optional(),
        description: zod_1.z.string().optional(),
        token_standard: zod_1.z.string().optional(),
        attributes: zod_1.z.array(zod_1.z.object({ trait_type: zod_1.z.string(), value: zod_1.z.string() })).optional(),
    }).optional(),
    files: zod_1.z.array(zod_1.z.object({ uri: zod_1.z.string().optional(), mime: zod_1.z.string().optional() })).optional(),
    links: zod_1.z.record(zod_1.z.string(), zod_1.z.string()).optional(),
});
const zDasOwnership = zod_1.z.object({
    owner: zPubkey,
    frozen: zod_1.z.boolean(),
    delegated: zod_1.z.boolean(),
    delegate: zPubkey.nullable(),
    ownership_model: zod_1.z.enum(['single', 'token']),
});
const zDasAsset = zod_1.z.object({
    interface: zod_1.z.string(),
    id: zPubkey,
    content: zDasContent,
    authorities: zod_1.z.array(zod_1.z.object({ address: zPubkey, scopes: zod_1.z.array(zod_1.z.string()) })),
    compression: zod_1.z.object({
        compressed: zod_1.z.boolean(),
        data_hash: zod_1.z.string(),
        creator_hash: zod_1.z.string(),
        asset_hash: zod_1.z.string(),
        tree: zPubkey,
        seq: zod_1.z.number(),
        leaf_id: zod_1.z.number(),
    }),
    grouping: zod_1.z.array(zod_1.z.object({ group_key: zod_1.z.string(), group_value: zod_1.z.string() })),
    royalty: zod_1.z.object({
        royalty_model: zod_1.z.string(),
        percent: zod_1.z.number(),
        basis_points: zod_1.z.number(),
        primary_sale_happened: zod_1.z.boolean(),
        locked: zod_1.z.boolean(),
    }),
    creators: zod_1.z.array(zod_1.z.object({ address: zPubkey, share: zod_1.z.number(), verified: zod_1.z.boolean() })),
    ownership: zDasOwnership,
    mutable: zod_1.z.boolean(),
    burnt: zod_1.z.boolean(),
    token_info: zod_1.z.object({
        symbol: zod_1.z.string().optional(),
        decimals: zod_1.z.number().optional(),
        supply: zod_1.z.number().optional(),
        balance: zod_1.z.number().optional(),
        price_info: zod_1.z.object({
            price_per_token: zod_1.z.number().optional(),
            currency: zod_1.z.string().optional(),
        }).optional(),
    }).optional(),
});
const zDasPage = zod_1.z.object({
    total: zod_1.z.number(),
    limit: zod_1.z.number(),
    page: zod_1.z.number(),
    cursor: zod_1.z.string().optional(),
    items: zod_1.z.array(zDasAsset),
});
const zDasProof = zod_1.z.object({
    root: zod_1.z.string(),
    proof: zod_1.z.array(zod_1.z.string()),
    node_index: zod_1.z.number(),
    leaf: zod_1.z.string(),
    tree_id: zPubkey,
});
/* ═══════════════════════════════════════════════════════════════
 *  1. DAS Core Methods
 * ═══════════════════════════════════════════════════════════════ */
register('getAsset', zod_1.z.object({
    id: zPubkey.describe('Asset ID (mint address or asset ID)'),
    commitment: zCommitment,
}), zDasAsset, 'Get complete metadata, ownership, compression, and royalty info for a single digital asset (NFT, cNFT, Core, or fungible).');
register('getAssets', zod_1.z.object({
    ids: zod_1.z.array(zPubkey).min(1).max(1000).describe('Asset IDs to fetch'),
    commitment: zCommitment,
}), zod_1.z.array(zDasAsset), 'Batch-fetch metadata for multiple digital assets in a single RPC call.');
register('getAssetProof', zod_1.z.object({
    id: zPubkey.describe('Compressed NFT asset ID'),
    commitment: zCommitment,
}), zDasProof, 'Get the Merkle tree proof for a compressed NFT. Required for transfers and burns of cNFTs.');
register('getAssetProofs', zod_1.z.object({
    ids: zod_1.z.array(zPubkey).min(1).max(1000).describe('Compressed NFT asset IDs'),
    commitment: zCommitment,
}), zod_1.z.record(zPubkey, zDasProof), 'Batch-fetch Merkle tree proofs for multiple compressed NFTs.');
register('getAssetsByOwner', zod_1.z.object({
    ownerAddress: zPubkey.describe('Wallet address of the owner'),
    ...zPagination,
    sortBy: zSortBy,
    ...zDisplayOpts,
    commitment: zCommitment,
}), zDasPage, 'Get all digital assets (NFTs, cNFTs, fungibles) owned by a wallet address.');
register('getAssetsByCreator', zod_1.z.object({
    creatorAddress: zPubkey.describe('Creator address'),
    onlyVerified: zod_1.z.boolean().optional().describe('Only return assets with verified creator'),
    ...zPagination,
    sortBy: zSortBy,
    ...zDisplayOpts,
    commitment: zCommitment,
}), zDasPage, 'Get all digital assets created by a specific creator address.');
register('getAssetsByCollection', zod_1.z.object({
    groupKey: zod_1.z.literal('collection').default('collection'),
    groupValue: zPubkey.describe('Collection address'),
    ...zPagination,
    sortBy: zSortBy,
    ...zDisplayOpts,
    commitment: zCommitment,
}), zDasPage, 'Get all assets in a Metaplex collection. Uses DAS getAssetsByGroup with groupKey="collection".');
register('getAssetsByAuthority', zod_1.z.object({
    authorityAddress: zPubkey.describe('Authority address'),
    ...zPagination,
    sortBy: zSortBy,
    commitment: zCommitment,
}), zDasPage, 'Get all digital assets managed by a specific authority address.');
register('searchAssets', zod_1.z.object({
    ownerAddress: zPubkey.optional(),
    creatorAddress: zPubkey.optional(),
    creatorVerified: zod_1.z.boolean().optional(),
    authorityAddress: zPubkey.optional(),
    grouping: zod_1.z.tuple([zod_1.z.string(), zod_1.z.string()]).optional().describe('[groupKey, groupValue] pair'),
    burnt: zod_1.z.boolean().optional(),
    compressed: zod_1.z.boolean().optional(),
    frozen: zod_1.z.boolean().optional(),
    name: zod_1.z.string().optional().describe('Search by asset name (substring match)'),
    tokenType: zod_1.z.enum(['fungible', 'nonFungible', 'regularNft', 'compressedNft', 'all']).optional(),
    interface: zod_1.z.string().optional().describe('Asset interface (e.g. "V1_NFT", "MplCoreAsset")'),
    jsonUri: zod_1.z.string().optional().describe('Filter by off-chain JSON URI'),
    ...zPagination,
    sortBy: zSortBy,
    ...zDisplayOpts,
    commitment: zCommitment,
}), zDasPage, 'Advanced multi-filter search across all indexed digital assets (NFTs, cNFTs, fungibles, Core assets).');
register('getAssetSignatures', zod_1.z.object({
    id: zPubkey.describe('Asset ID to get transaction history for'),
    ...zPagination,
    sortDirection: zod_1.z.enum(['asc', 'desc']).optional(),
    commitment: zCommitment,
}), zod_1.z.object({
    total: zod_1.z.number(),
    limit: zod_1.z.number(),
    page: zod_1.z.number(),
    items: zod_1.z.array(zod_1.z.object({
        signature: zod_1.z.string(),
        type: zod_1.z.string().optional(),
        slot: zod_1.z.number().optional(),
    })),
}), 'Get the transaction history (signatures) for a specific compressed NFT.');
register('getTokenAccounts', zod_1.z.object({
    owner: zPubkey.optional().describe('Token account owner'),
    mint: zPubkey.optional().describe('Filter by token mint'),
    ...zPagination,
    showZeroBalance: zod_1.z.boolean().optional(),
    commitment: zCommitment,
}), zod_1.z.object({
    total: zod_1.z.number(),
    limit: zod_1.z.number(),
    page: zod_1.z.number(),
    token_accounts: zod_1.z.array(zod_1.z.object({
        address: zPubkey,
        mint: zPubkey,
        owner: zPubkey,
        amount: zod_1.z.number(),
        delegated_amount: zod_1.z.number().optional(),
        frozen: zod_1.z.boolean().optional(),
    })),
}), 'Get SPL / Token-2022 token accounts via DAS, with optional filters.');
/* ═══════════════════════════════════════════════════════════════
 *  2. Higher-level Helpers
 * ═══════════════════════════════════════════════════════════════ */
register('resolveCollection', zod_1.z.object({
    collectionAddress: zPubkey.describe('Collection mint / address'),
    sampleSize: zod_1.z.number().int().positive().max(50).optional()
        .describe('Number of sample assets to fetch for trait analysis (default 10)'),
    commitment: zCommitment,
}), zod_1.z.object({
    collection: zDasAsset.describe('Collection asset metadata'),
    stats: zod_1.z.object({
        totalSupply: zod_1.z.number(),
        sampleSize: zod_1.z.number(),
        uniqueOwners: zod_1.z.number(),
        compressedCount: zod_1.z.number(),
        burnedCount: zod_1.z.number(),
        traitSummary: zod_1.z.record(zod_1.z.string(), zod_1.z.array(zod_1.z.string())).optional(),
    }),
}), 'Resolve a collection address into full metadata + computed stats (supply, owners, traits).');
/* ═══════════════════════════════════════════════════════════════
 *  Export
 * ═══════════════════════════════════════════════════════════════ */
/**
 * @description All 12 registered Metaplex methods with typed Zod schemas.
 * @since 1.0.0
 */
exports.metaplexMethods = methods;
/**
 * @description Metaplex method names as a readonly tuple for allow-listing.
 * @since 1.0.0
 */
exports.metaplexMethodNames = methods.map((m) => m.name);
//# sourceMappingURL=schemas.js.map