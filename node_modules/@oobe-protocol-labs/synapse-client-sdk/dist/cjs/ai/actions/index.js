"use strict";
/**
 * @module ai/actions
 * @description Solana Actions & Blinks — Turn agent tools into shareable URLs.
 *
 * Sub-modules:
 *  - `types`   — Solana Actions spec types + Synapse extensions
 *  - `server`  — Framework-agnostic HTTP request handler
 *  - `blinks`  — Blink URL generation and social metadata
 *
 * @example
 * ```ts
 * import {
 *   ActionServer,
 *   BlinkGenerator,
 *   createBlinkFromAction,
 * } from '@oobe-protocol-labs/synapse-client-sdk/ai';
 *
 * // 1. Create server
 * const server = new ActionServer({ baseUrl: 'https://myagent.xyz' });
 *
 * // 2. Define actions
 * server.defineAction({
 *   id: 'swap',
 *   icon: 'https://myagent.xyz/icon.png',
 *   title: 'Swap via Jupiter',
 *   description: 'Swap any SPL token with best price',
 *   label: 'Swap Now',
 *   parameters: [
 *     { name: 'from', label: 'From Token' },
 *     { name: 'to', label: 'To Token' },
 *     { name: 'amount', label: 'Amount', type: 'number' },
 *   ],
 *   handler: async (ctx) => ({
 *     transaction: await buildSwapTx(ctx.account, ctx.params),
 *   }),
 * });
 *
 * // 3. Generate Blink
 * const blinks = new BlinkGenerator({
 *   actionUrl: 'https://myagent.xyz/api/actions/swap',
 * });
 * const shareUrl = blinks.createUrl({ from: 'SOL', to: 'USDC', amount: '1000000000' });
 * // → Share on Twitter, Discord, etc.
 * ```
 *
 * @since 1.3.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ACTION_SCHEME = exports.DEFAULT_RESOLVER_URL = exports.createBlinkFromAction = exports.BlinkGenerator = exports.ActionServerError = exports.ActionServer = void 0;
/* ── Server ── */
var server_1 = require("./server");
Object.defineProperty(exports, "ActionServer", { enumerable: true, get: function () { return server_1.ActionServer; } });
Object.defineProperty(exports, "ActionServerError", { enumerable: true, get: function () { return server_1.ActionServerError; } });
/* ── Blinks ── */
var blinks_1 = require("./blinks");
Object.defineProperty(exports, "BlinkGenerator", { enumerable: true, get: function () { return blinks_1.BlinkGenerator; } });
Object.defineProperty(exports, "createBlinkFromAction", { enumerable: true, get: function () { return blinks_1.createBlinkFromAction; } });
Object.defineProperty(exports, "DEFAULT_RESOLVER_URL", { enumerable: true, get: function () { return blinks_1.DEFAULT_RESOLVER_URL; } });
Object.defineProperty(exports, "ACTION_SCHEME", { enumerable: true, get: function () { return blinks_1.ACTION_SCHEME; } });
//# sourceMappingURL=index.js.map