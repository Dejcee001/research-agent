"use strict";
/**
 * SPL Token Program instruction encoders.
 *
 * Encodes the most common SPL Token v1 instructions. Also works for
 * Token-2022 when using the basic instruction set (the program ID is
 * the only difference — pass `TOKEN_2022_PROGRAM` as `programId`).
 *
 * Program ID (v1): `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA`
 * Program ID (2022): `TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb`
 *
 * @module programs/spl-token
 * @since 1.1.0
 *
 * @example
 * ```ts
 * import { SplToken } from '@oobe-protocol-labs/synapse-client-sdk/programs';
 * import { Pubkey } from '@oobe-protocol-labs/synapse-client-sdk';
 *
 * const ix = SplToken.transferChecked({
 *   source: Pubkey('SourceTokenAccount...'),
 *   mint: Pubkey('TokenMint...'),
 *   destination: Pubkey('DestTokenAccount...'),
 *   owner: Pubkey('WalletOwner...'),
 *   amount: 1_000_000n,
 *   decimals: 6,
 * });
 * ```
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SplToken = exports.TOKEN_2022_PROGRAM = exports.TOKEN_PROGRAM = void 0;
const types_1 = require("../core/types");
const types_2 = require("./types");
// ── Program IDs ────────────────────────────────────────────────
/**
 * SPL Token Program v1 address.
 * @since 1.1.0
 */
exports.TOKEN_PROGRAM = (0, types_1.Pubkey)('TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA');
/**
 * SPL Token-2022 (Token Extensions) Program address.
 * @since 1.1.0
 */
exports.TOKEN_2022_PROGRAM = (0, types_1.Pubkey)('TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb');
// ── Sysvar ─────────────────────────────────────────────────────
/** @internal */
const RENT_SYSVAR = (0, types_1.Pubkey)('SysvarRent111111111111111111111111111111111');
// ── Helpers ────────────────────────────────────────────────────
/** @internal Build owner key(s) — single signer or multisig */
function ownerKeys(owner, multiSigners, ownerWritable = false) {
    if (multiSigners && multiSigners.length > 0) {
        return [
            ownerWritable ? (0, types_2.writable)(owner) : (0, types_2.readonly)(owner),
            ...multiSigners.map(s => (0, types_2.readonlySigner)(s)),
        ];
    }
    return [ownerWritable ? (0, types_2.writableSigner)(owner) : (0, types_2.readonlySigner)(owner)];
}
// ── SplToken namespace ─────────────────────────────────────────
/**
 * SPL Token Program instruction encoders.
 *
 * Static methods produce {@link TransactionInstruction} objects for the
 * SPL Token Program (v1) and Token-2022.
 *
 * @since 1.1.0
 */
exports.SplToken = {
    /** SPL Token v1 program ID. */
    programId: exports.TOKEN_PROGRAM,
    /** Token-2022 program ID. */
    token2022ProgramId: exports.TOKEN_2022_PROGRAM,
    /**
     * Initialize a new token mint.
     * @since 1.1.0
     */
    initializeMint(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const hasFreezeAuth = p.freezeAuthority != null;
        const w = new types_2.InstructionWriter(67);
        w.u8(0 /* TokenIx.InitializeMint */);
        w.u8(p.decimals);
        w.pubkey(p.mintAuthority);
        w.u8(hasFreezeAuth ? 1 : 0); // COption tag
        if (hasFreezeAuth) {
            w.pubkey(p.freezeAuthority);
        }
        else {
            w.raw(new Uint8Array(32)); // padding
        }
        return {
            programId: pid,
            keys: [(0, types_2.writable)(p.mint), (0, types_2.readonly)(RENT_SYSVAR)],
            data: w.toBytes(),
        };
    },
    /**
     * Initialize a token account for a given mint and owner.
     * @since 1.1.0
     */
    initializeAccount(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(1);
        w.u8(1 /* TokenIx.InitializeAccount */);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.readonly)(p.mint),
                (0, types_2.readonly)(p.owner),
                (0, types_2.readonly)(RENT_SYSVAR),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Transfer tokens (unchecked — no decimal verification).
     * Prefer {@link transferChecked} for safety.
     * @since 1.1.0
     */
    transfer(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(9);
        w.u8(3 /* TokenIx.Transfer */);
        w.u64(p.amount);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.source),
                (0, types_2.writable)(p.destination),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Transfer tokens with decimal verification (recommended).
     * @since 1.1.0
     */
    transferChecked(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(10);
        w.u8(12 /* TokenIx.TransferChecked */);
        w.u64(p.amount);
        w.u8(p.decimals);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.source),
                (0, types_2.readonly)(p.mint),
                (0, types_2.writable)(p.destination),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Approve a delegate to spend tokens from the account.
     * @since 1.1.0
     */
    approve(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(9);
        w.u8(4 /* TokenIx.Approve */);
        w.u64(p.amount);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.readonly)(p.delegate),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Approve a delegate with decimal verification.
     * @since 1.1.0
     */
    approveChecked(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(10);
        w.u8(13 /* TokenIx.ApproveChecked */);
        w.u64(p.amount);
        w.u8(p.decimals);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.readonly)(p.mint),
                (0, types_2.readonly)(p.delegate),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Revoke a previously approved delegate.
     * @since 1.1.0
     */
    revoke(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(1);
        w.u8(5 /* TokenIx.Revoke */);
        return {
            programId: pid,
            keys: [(0, types_2.writable)(p.account), ...ownerKeys(p.owner, p.multiSigners)],
            data: w.toBytes(),
        };
    },
    /**
     * Mint new tokens to a destination account.
     * @since 1.1.0
     */
    mintTo(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(9);
        w.u8(7 /* TokenIx.MintTo */);
        w.u64(p.amount);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.mint),
                (0, types_2.writable)(p.destination),
                ...ownerKeys(p.authority, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Mint new tokens with decimal verification.
     * @since 1.1.0
     */
    mintToChecked(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(10);
        w.u8(14 /* TokenIx.MintToChecked */);
        w.u64(p.amount);
        w.u8(p.decimals);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.mint),
                (0, types_2.writable)(p.destination),
                ...ownerKeys(p.authority, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Burn tokens from an account.
     * @since 1.1.0
     */
    burn(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(9);
        w.u8(8 /* TokenIx.Burn */);
        w.u64(p.amount);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.writable)(p.mint),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Burn tokens with decimal verification.
     * @since 1.1.0
     */
    burnChecked(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(10);
        w.u8(15 /* TokenIx.BurnChecked */);
        w.u64(p.amount);
        w.u8(p.decimals);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.writable)(p.mint),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Close a token account, reclaiming SOL rent.
     * @since 1.1.0
     */
    closeAccount(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(1);
        w.u8(9 /* TokenIx.CloseAccount */);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.writable)(p.destination),
                ...ownerKeys(p.owner, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Freeze a token account (prevents transfers).
     * @since 1.1.0
     */
    freezeAccount(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(1);
        w.u8(10 /* TokenIx.FreezeAccount */);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.readonly)(p.mint),
                ...ownerKeys(p.authority, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Thaw a previously frozen token account.
     * @since 1.1.0
     */
    thawAccount(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(1);
        w.u8(11 /* TokenIx.ThawAccount */);
        return {
            programId: pid,
            keys: [
                (0, types_2.writable)(p.account),
                (0, types_2.readonly)(p.mint),
                ...ownerKeys(p.authority, p.multiSigners),
            ],
            data: w.toBytes(),
        };
    },
    /**
     * Sync the lamport balance of a native SOL token account.
     * @since 1.1.0
     */
    syncNative(p) {
        const pid = p.programId ?? exports.TOKEN_PROGRAM;
        const w = new types_2.InstructionWriter(1);
        w.u8(17 /* TokenIx.SyncNative */);
        return {
            programId: pid,
            keys: [(0, types_2.writable)(p.account)],
            data: w.toBytes(),
        };
    },
};
//# sourceMappingURL=spl-token.js.map