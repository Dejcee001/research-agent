import { MCP_PROTOCOL_VERSION, MCP_JSONRPC_VERSION } from './types.js';
/* ═══════════════════════════════════════════════════════════════
 *  MCP Errors
 * ═══════════════════════════════════════════════════════════════ */
const MCP_ERRORS = {
    PARSE_ERROR: { code: -32700, message: 'Parse error' },
    INVALID_REQUEST: { code: -32600, message: 'Invalid Request' },
    METHOD_NOT_FOUND: { code: -32601, message: 'Method not found' },
    INVALID_PARAMS: { code: -32602, message: 'Invalid params' },
    INTERNAL_ERROR: { code: -32603, message: 'Internal error' },
};
export class McpServerError extends Error {
    code;
    data;
    constructor(message, code = -32603, data) {
        super(message);
        this.code = code;
        this.data = data;
        this.name = 'McpServerError';
    }
}
/* ═══════════════════════════════════════════════════════════════
 *  Zod → JSON Schema (deep conversion)
 * ═══════════════════════════════════════════════════════════════ */
function zodToMcpSchema(schema) {
    const result = convertZod(schema);
    if (result.type === 'object')
        return result;
    return { type: 'object', properties: { value: result } };
}
function convertZod(z) {
    if (!z || typeof z !== 'object')
        return { type: 'string' };
    const def = z?._def ?? z?.def;
    if (!def)
        return { type: 'string' };
    const typeName = def.typeName ?? def.type;
    const description = z?.description ?? def?.description;
    switch (typeName) {
        case 'ZodString': {
            const base = { type: 'string' };
            if (description)
                base.description = description;
            if (def.checks) {
                for (const check of def.checks) {
                    if (check.kind === 'min')
                        base.minLength = check.value;
                    if (check.kind === 'max')
                        base.maxLength = check.value;
                    if (check.kind === 'url')
                        base.format = 'uri';
                    if (check.kind === 'email')
                        base.format = 'email';
                }
            }
            return base;
        }
        case 'ZodNumber': {
            const base = { type: 'number' };
            if (description)
                base.description = description;
            if (def.checks) {
                for (const check of def.checks) {
                    if (check.kind === 'min')
                        base.minimum = check.value;
                    if (check.kind === 'max')
                        base.maximum = check.value;
                    if (check.kind === 'int')
                        base.type = 'integer';
                }
            }
            return base;
        }
        case 'ZodBoolean': {
            const base = { type: 'boolean' };
            if (description)
                base.description = description;
            return base;
        }
        case 'ZodArray': {
            const base = {
                type: 'array',
                items: convertZod(def.type),
            };
            if (description)
                base.description = description;
            return base;
        }
        case 'ZodObject': {
            const shape = z.shape ?? def.shape?.();
            const properties = {};
            const required = [];
            if (shape) {
                for (const [key, val] of Object.entries(shape)) {
                    properties[key] = convertZod(val);
                    const v = val;
                    const isOpt = v?._def?.typeName === 'ZodOptional' ||
                        v?._def?.typeName === 'ZodDefault' ||
                        v?.isOptional?.() === true;
                    if (!isOpt)
                        required.push(key);
                }
            }
            const base = { type: 'object', properties };
            if (required.length > 0)
                base.required = required;
            if (description)
                base.description = description;
            return base;
        }
        case 'ZodEnum': {
            const values = def.values ?? def.entries;
            const base = { type: 'string' };
            if (Array.isArray(values))
                base.enum = values;
            if (description)
                base.description = description;
            return base;
        }
        case 'ZodOptional':
            return convertZod(def.innerType);
        case 'ZodDefault':
            return { ...convertZod(def.innerType), default: def.defaultValue?.() };
        case 'ZodNullable': {
            const inner = convertZod(def.innerType);
            return { ...inner, nullable: true };
        }
        case 'ZodRecord': {
            const base = {
                type: 'object',
                additionalProperties: convertZod(def.valueType),
            };
            if (description)
                base.description = description;
            return base;
        }
        case 'ZodTuple': {
            const items = (def.items ?? []).map((i) => convertZod(i));
            const base = { type: 'array', items, minItems: items.length, maxItems: items.length };
            if (description)
                base.description = description;
            return base;
        }
        case 'ZodLiteral': {
            return { type: typeof def.value === 'number' ? 'number' : 'string', const: def.value };
        }
        case 'ZodUnion': {
            const options = (def.options ?? []).map((o) => convertZod(o));
            return { oneOf: options };
        }
        default: {
            const base = { type: 'string' };
            if (description)
                base.description = description;
            return base;
        }
    }
}
/* ═══════════════════════════════════════════════════════════════
 *  SynapseMcpServer
 * ═══════════════════════════════════════════════════════════════ */
export class SynapseMcpServer {
    kit;
    config;
    toolDefs;
    resourceDefs;
    resourceTemplates;
    _initialized = false;
    _running = false;
    _stdinBuffer = '';
    // SSE state
    _httpServer = null;
    _sseClients = new Map();
    constructor(kit, config = {}) {
        this.kit = kit;
        this.config = {
            name: config.name ?? 'synapse-solana-mcp',
            version: config.version ?? '2.0.0',
            ...config,
        };
        // Pre-build tool definitions from kit
        this.toolDefs = this.buildToolDefinitions();
        this.resourceDefs = this.buildResourceDefinitions();
        this.resourceTemplates = this.buildResourceTemplates();
    }
    /* ── Lifecycle ────────────────────────────────────────────── */
    /**
     * Start the MCP server on the configured transport.
     */
    async start() {
        if (this._running)
            return;
        this._running = true;
        const transport = this.config.transport ?? 'stdio';
        if (transport === 'stdio') {
            await this.startStdio();
        }
        else if (transport === 'sse') {
            await this.startSse();
        }
        else {
            throw new McpServerError(`Unknown transport: ${transport}`);
        }
    }
    /**
     * Stop the MCP server.
     */
    async stop() {
        this._running = false;
        if (this._httpServer) {
            await new Promise((resolve) => {
                this._httpServer.close(() => resolve());
            });
            this._httpServer = null;
        }
        // Cleanup SSE clients
        for (const [, client] of this._sseClients) {
            try {
                client.res.end();
            }
            catch { /* noop */ }
        }
        this._sseClients.clear();
    }
    /* ── stdio Transport ──────────────────────────────────────── */
    async startStdio() {
        const stdin = process.stdin;
        const stdout = process.stdout;
        stdin.setEncoding('utf8');
        stdin.resume();
        stdin.on('data', (chunk) => {
            this._stdinBuffer += chunk;
            this.processStdinBuffer(stdout);
        });
        stdin.on('end', () => {
            this._running = false;
        });
        if (this.config.debug) {
            process.stderr.write(`[MCP] ${this.config.name} listening on stdio\n`);
        }
    }
    processStdinBuffer(stdout) {
        // MCP uses newline-delimited JSON
        while (true) {
            const newlineIdx = this._stdinBuffer.indexOf('\n');
            if (newlineIdx === -1)
                break;
            const line = this._stdinBuffer.slice(0, newlineIdx).trim();
            this._stdinBuffer = this._stdinBuffer.slice(newlineIdx + 1);
            if (!line)
                continue;
            this.handleMessage(line)
                .then((response) => {
                if (response) {
                    stdout.write(JSON.stringify(response) + '\n');
                }
            })
                .catch((err) => {
                if (this.config.debug) {
                    process.stderr.write(`[MCP] Error: ${err}\n`);
                }
            });
        }
    }
    /* ── SSE Transport ────────────────────────────────────────── */
    async startSse() {
        const http = await import('http');
        const port = this.config.ssePort ?? 3001;
        const basePath = (this.config.ssePath ?? '/mcp').replace(/\/$/, '');
        this._httpServer = http.createServer(async (req, res) => {
            const url = new URL(req.url ?? '/', `http://localhost:${port}`);
            const path = url.pathname;
            // CORS headers
            res.setHeader('Access-Control-Allow-Origin', '*');
            res.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS');
            res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
            if (req.method === 'OPTIONS') {
                res.writeHead(204);
                res.end();
                return;
            }
            // SSE endpoint — client subscribes here
            if (path === `${basePath}/sse` && req.method === 'GET') {
                const clientId = crypto.randomUUID();
                res.writeHead(200, {
                    'Content-Type': 'text/event-stream',
                    'Cache-Control': 'no-cache',
                    Connection: 'keep-alive',
                });
                // Send the endpoint URL for posting messages
                const messageUrl = `${basePath}/message?sessionId=${clientId}`;
                res.write(`event: endpoint\ndata: ${messageUrl}\n\n`);
                this._sseClients.set(clientId, { res, id: clientId });
                req.on('close', () => {
                    this._sseClients.delete(clientId);
                });
                if (this.config.debug) {
                    process.stderr.write(`[MCP] SSE client connected: ${clientId}\n`);
                }
                return;
            }
            // Message endpoint — client sends JSON-RPC messages here
            if (path === `${basePath}/message` && req.method === 'POST') {
                const sessionId = url.searchParams.get('sessionId');
                const client = sessionId ? this._sseClients.get(sessionId) : undefined;
                if (!client) {
                    res.writeHead(404, { 'Content-Type': 'application/json' });
                    res.end(JSON.stringify({ error: 'Session not found' }));
                    return;
                }
                let body = '';
                for await (const chunk of req) {
                    body += chunk;
                }
                try {
                    const response = await this.handleMessage(body);
                    if (response) {
                        // Send response via SSE
                        client.res.write(`event: message\ndata: ${JSON.stringify(response)}\n\n`);
                    }
                    res.writeHead(202, { 'Content-Type': 'application/json' });
                    res.end(JSON.stringify({ ok: true }));
                }
                catch (err) {
                    res.writeHead(400, { 'Content-Type': 'application/json' });
                    res.end(JSON.stringify({ error: String(err) }));
                }
                return;
            }
            // Health check
            if (path === `${basePath}/health`) {
                res.writeHead(200, { 'Content-Type': 'application/json' });
                res.end(JSON.stringify({
                    status: 'ok',
                    server: this.config.name,
                    version: this.config.version,
                    tools: this.toolDefs.length,
                    resources: this.resourceDefs.length,
                    connectedClients: this._sseClients.size,
                }));
                return;
            }
            res.writeHead(404);
            res.end('Not found');
        });
        await new Promise((resolve) => {
            this._httpServer.listen(port, () => {
                if (this.config.debug) {
                    process.stderr.write(`[MCP] ${this.config.name} SSE server on http://localhost:${port}${basePath}\n`);
                }
                resolve();
            });
        });
    }
    /* ── Message Handling ─────────────────────────────────────── */
    async handleMessage(raw) {
        let parsed;
        try {
            parsed = JSON.parse(raw);
        }
        catch {
            return this.errorResponse(null, MCP_ERRORS.PARSE_ERROR.code, MCP_ERRORS.PARSE_ERROR.message);
        }
        if (!parsed.jsonrpc || parsed.jsonrpc !== '2.0') {
            return this.errorResponse(parsed.id ?? null, MCP_ERRORS.INVALID_REQUEST.code, MCP_ERRORS.INVALID_REQUEST.message);
        }
        // Notifications (no id) — don't respond
        if (parsed.id === undefined || parsed.id === null) {
            await this.handleNotification(parsed);
            return null;
        }
        if (this.config.debug) {
            process.stderr.write(`[MCP] ← ${parsed.method}\n`);
        }
        try {
            const result = await this.dispatch(parsed);
            return {
                jsonrpc: MCP_JSONRPC_VERSION,
                id: parsed.id,
                result,
            };
        }
        catch (err) {
            const mcpErr = err instanceof McpServerError ? err : new McpServerError(String(err));
            return this.errorResponse(parsed.id, mcpErr.code, mcpErr.message, mcpErr.data);
        }
    }
    async dispatch(req) {
        switch (req.method) {
            case 'initialize':
                return this.handleInitialize(req.params);
            case 'initialized':
                return {}; // acknowledgment
            case 'ping':
                return {}; // pong
            case 'tools/list':
                return this.handleToolsList(req.params);
            case 'tools/call':
                return this.handleToolsCall(req.params);
            case 'resources/list':
                return this.handleResourcesList(req.params);
            case 'resources/read':
                return this.handleResourcesRead(req.params);
            case 'resources/templates/list':
                return this.handleResourceTemplatesList();
            case 'prompts/list':
                return { prompts: [] }; // No prompts for now
            case 'prompts/get':
                throw new McpServerError('Prompt not found', MCP_ERRORS.INVALID_PARAMS.code);
            case 'completion/complete':
                return { completion: { values: [], hasMore: false, total: 0 } };
            case 'logging/setLevel':
                return {};
            default:
                throw new McpServerError(`Unknown method: ${req.method}`, MCP_ERRORS.METHOD_NOT_FOUND.code);
        }
    }
    async handleNotification(notif) {
        switch (notif.method) {
            case 'notifications/initialized':
                this._initialized = true;
                break;
            case 'notifications/cancelled':
                // Handle cancellation if needed
                break;
            default:
                if (this.config.debug) {
                    process.stderr.write(`[MCP] Unhandled notification: ${notif.method}\n`);
                }
        }
    }
    /* ── MCP Method Handlers ──────────────────────────────────── */
    handleInitialize(params) {
        this._initialized = true;
        const capabilities = {
            tools: { listChanged: false },
            resources: { subscribe: false, listChanged: false },
            ...this.config.capabilities,
        };
        const summary = this.kit.summary();
        return {
            protocolVersion: MCP_PROTOCOL_VERSION,
            capabilities,
            serverInfo: {
                name: this.config.name,
                version: this.config.version,
            },
            instructions: this.config.instructions ??
                `Synapse Solana MCP Server — ${summary.totalTools} tools across ${summary.totalProtocols} protocols. ` +
                    `Plugins: ${summary.plugins.map((p) => p.name).join(', ')}. ` +
                    `Provides full Solana blockchain access: tokens, NFTs, DeFi, staking, domains, oracles, and more.`,
        };
    }
    handleToolsList(_params) {
        return { tools: this.toolDefs };
    }
    async handleToolsCall(params) {
        const { name, arguments: args } = params;
        if (!name) {
            throw new McpServerError('Tool name is required', MCP_ERRORS.INVALID_PARAMS.code);
        }
        // Find the tool in the kit
        const toolMap = this.kit.getToolMap();
        const tool = toolMap[name];
        if (!tool) {
            throw new McpServerError(`Tool not found: "${name}". Available: ${Object.keys(toolMap).slice(0, 10).join(', ')}...`, MCP_ERRORS.INVALID_PARAMS.code);
        }
        try {
            // Invoke the LangChain tool
            const result = await tool.invoke(args ?? {});
            const text = typeof result === 'string' ? result : JSON.stringify(result, null, 2);
            return {
                content: [{ type: 'text', text }],
                isError: false,
            };
        }
        catch (err) {
            const errMsg = err instanceof Error ? err.message : String(err);
            return {
                content: [{ type: 'text', text: `Error executing ${name}: ${errMsg}` }],
                isError: true,
            };
        }
    }
    handleResourcesList(_params) {
        return { resources: this.resourceDefs };
    }
    async handleResourcesRead(params) {
        const { uri } = params;
        // Try to match resource templates and execute
        // For now, return a description of the resource
        return {
            contents: [{
                    uri,
                    mimeType: 'application/json',
                    text: JSON.stringify({
                        uri,
                        message: `Resource ${uri} — use the corresponding tool to fetch actual data.`,
                        availableTools: Object.keys(this.kit.getToolMap()).slice(0, 20),
                    }, null, 2),
                }],
        };
    }
    handleResourceTemplatesList() {
        return { resourceTemplates: this.resourceTemplates };
    }
    /* ── Builders ─────────────────────────────────────────────── */
    buildToolDefinitions() {
        const defs = [];
        const descriptors = this.kit.getMcpToolDescriptors();
        for (const desc of descriptors) {
            // Find the matching protocol method for deep schema conversion
            const pluginTools = this.kit.getPluginTools(desc.plugin);
            const method = pluginTools
                ? this.findMethodByName(desc.plugin, desc.name)
                : undefined;
            defs.push({
                name: desc.name,
                description: desc.description,
                inputSchema: method
                    ? zodToMcpSchema(method.input)
                    : desc.inputSchema,
            });
        }
        return defs;
    }
    buildResourceDefinitions() {
        return this.kit.getMcpResourceDescriptors().map((r) => ({
            uri: r.uri,
            name: r.name,
            description: r.description,
            mimeType: r.mimeType,
        }));
    }
    buildResourceTemplates() {
        // Convert resource URIs with {param} into templates
        return this.kit.getMcpResourceDescriptors()
            .filter((r) => r.uri.includes('{'))
            .map((r) => ({
            uriTemplate: r.uri,
            name: r.name,
            description: r.description,
            mimeType: r.mimeType,
        }));
    }
    findMethodByName(pluginId, toolName) {
        const summary = this.kit.summary();
        const pluginInfo = summary.plugins.find((p) => p.id === pluginId);
        if (!pluginInfo)
            return undefined;
        for (const protoId of pluginInfo.protocols) {
            const toolkit = this.kit.getProtocolToolkit(pluginId, protoId);
            if (toolkit) {
                const method = toolkit.methods.find((m) => `${protoId}_${m.name}` === toolName);
                if (method)
                    return method;
            }
        }
        return undefined;
    }
    /* ── Response Helpers ─────────────────────────────────────── */
    errorResponse(id, code, message, data) {
        return {
            jsonrpc: MCP_JSONRPC_VERSION,
            id: id ?? 0,
            error: { code, message, ...(data !== undefined ? { data } : {}) },
        };
    }
    /* ── Introspection ────────────────────────────────────────── */
    /**
     * Get server info and stats.
     */
    info() {
        return {
            name: this.config.name,
            version: this.config.version,
            transport: this.config.transport ?? 'stdio',
            tools: this.toolDefs.length,
            resources: this.resourceDefs.length,
            resourceTemplates: this.resourceTemplates.length,
            running: this._running,
            initialized: this._initialized,
        };
    }
}
//# sourceMappingURL=server.js.map