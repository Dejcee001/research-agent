/**
 * Known Solana program registry — maps program IDs to human-readable names.
 *
 * Covers system programs, SPL programs, major DeFi protocols (Jupiter,
 * Raydium, Meteora, Orca), NFT programs (Metaplex), and infrastructure.
 *
 * Use `resolveProgram()` for single lookups or `resolveProgramBatch()`
 * for bulk resolution. Both support custom program registries.
 *
 * @module grpc/parser/programs
 * @since 1.0.0
 */
declare const SYSTEM_PROGRAMS: Record<string, string>;
declare const SPL_PROGRAMS: Record<string, string>;
declare const JUPITER_PROGRAMS: Record<string, string>;
declare const RAYDIUM_PROGRAMS: Record<string, string>;
declare const METEORA_PROGRAMS: Record<string, string>;
declare const ORCA_PROGRAMS: Record<string, string>;
declare const METAPLEX_PROGRAMS: Record<string, string>;
declare const MARINADE_PROGRAMS: Record<string, string>;
declare const OTHER_PROGRAMS: Record<string, string>;
/** Complete program registry. Frozen at module load. @since 1.0.0 */
export declare const KNOWN_PROGRAMS: Readonly<Record<string, string>>;
export { SYSTEM_PROGRAMS, SPL_PROGRAMS, JUPITER_PROGRAMS, RAYDIUM_PROGRAMS, METEORA_PROGRAMS, ORCA_PROGRAMS, METAPLEX_PROGRAMS, MARINADE_PROGRAMS, OTHER_PROGRAMS, };
/**
 * Resolve a single program ID to a human-readable name.
 *
 * @param programId - Base58 program ID.
 * @param custom - Optional extra registry (takes priority over built-in).
 * @returns The name if known, otherwise `undefined`.
 *
 * @example
 * ```ts
 * resolveProgram('JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4');
 * // => 'Jupiter Aggregator v6'
 * ```
 * @since 1.0.0
 */
export declare function resolveProgram(programId: string, custom?: Record<string, string>): string | undefined;
/**
 * Resolve multiple program IDs at once.
 *
 * @param programIds - Array of base58 program IDs.
 * @param custom - Optional extra registry.
 * @returns Map of programId → name (only known programs included).
 * @since 1.0.0
 */
export declare function resolveProgramBatch(programIds: readonly string[], custom?: Record<string, string>): Map<string, string>;
/**
 * Check if a program ID belongs to a specific protocol category.
 *
 * @example
 * ```ts
 * isProgramInCategory('JUP6...', 'jupiter'); // true
 * isProgramInCategory('whirL...', 'raydium'); // false
 * ```
 * @since 1.0.0
 */
export declare function isProgramInCategory(programId: string, category: 'system' | 'spl' | 'jupiter' | 'raydium' | 'meteora' | 'orca' | 'metaplex' | 'marinade'): boolean;
/**
 * Get the protocol category of a program ID.
 *
 * @returns Category name or `'unknown'`.
 * @since 1.0.0
 */
export declare function getProgramCategory(programId: string): 'system' | 'spl' | 'jupiter' | 'raydium' | 'meteora' | 'orca' | 'metaplex' | 'marinade' | 'other' | 'unknown';
/** Total number of known programs. @since 1.0.0 */
export declare const KNOWN_PROGRAMS_COUNT: number;
//# sourceMappingURL=programs.d.ts.map