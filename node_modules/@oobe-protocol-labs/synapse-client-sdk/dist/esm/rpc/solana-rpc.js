import * as m from './methods/index.js';
/**
 * Typed wrapper around the full Solana JSON-RPC API surface.
 *
 * Every method is a thin one-liner that delegates to the corresponding
 * standalone function in `./methods`, keeping this class easy to maintain
 * and the standalone functions tree-shakeable.
 *
 * @example
 * ```ts
 * const rpc = new SolanaRpc(transport);
 * const balance = await rpc.getBalance('So11...');
 * const block = await rpc.getBlock(123456);
 * ```
 * @since 1.0.0
 */
export class SolanaRpc {
    t;
    /** @param t - The HTTP transport used for all JSON-RPC calls. @since 1.0.0 */
    constructor(t) {
        this.t = t;
    }
    // ── Account ────────────────────────────────────────────────────
    /** Fetch account information for a public key. @since 1.0.0 */
    getAccountInfo(pubkey, opts) { return m.getAccountInfo(this.t, pubkey, opts); }
    /** Fetch the SOL balance for a public key. @since 1.0.0 */
    getBalance(pubkey, commitment, opts) { return m.getBalance(this.t, pubkey, commitment, opts); }
    /** Fetch account information for multiple public keys in one request. @since 1.0.0 */
    getMultipleAccounts(pubkeys, opts) { return m.getMultipleAccounts(this.t, pubkeys, opts); }
    /** Fetch all accounts owned by a program. @since 1.0.0 */
    getProgramAccounts(programId, opts) { return m.getProgramAccounts(this.t, programId, opts); }
    // ── Block ──────────────────────────────────────────────────────
    /** Fetch a confirmed block by slot number. @since 1.0.0 */
    getBlock(slot, opts) { return m.getBlock(this.t, slot, opts); }
    /** Get the current block height of the node. @since 1.0.0 */
    getBlockHeight(commitment, opts) { return m.getBlockHeight(this.t, commitment, opts); }
    /** Get the estimated production time of a block. @since 1.0.0 */
    getBlockTime(slot, opts) { return m.getBlockTime(this.t, slot, opts); }
    /** Get recent block production information. @since 1.0.0 */
    getBlockProduction(opts) { return m.getBlockProduction(this.t, opts); }
    /** Get a list of confirmed blocks between two slots. @since 1.0.0 */
    getBlocks(startSlot, endSlot, commitment, opts) { return m.getBlocks(this.t, startSlot, endSlot, commitment, opts); }
    /** Get confirmed blocks starting from a slot, with a limit. @since 1.0.0 */
    getBlocksWithLimit(startSlot, limit, commitment, opts) { return m.getBlocksWithLimit(this.t, startSlot, limit, commitment, opts); }
    /** Get the commitment for a particular block. @since 1.0.0 */
    getBlockCommitment(slot, opts) { return m.getBlockCommitment(this.t, slot, opts); }
    // ── Transaction ────────────────────────────────────────────────
    /** Fetch a confirmed transaction by its signature. @since 1.0.0 */
    getTransaction(signature, opts) { return m.getTransaction(this.t, signature, opts); }
    /** Get confirmed signatures for transactions involving an address. @since 1.0.0 */
    getSignaturesForAddress(address, opts) { return m.getSignaturesForAddress(this.t, address, opts); }
    /** Get the statuses of a list of transaction signatures. @since 1.0.0 */
    getSignatureStatuses(signatures, opts) { return m.getSignatureStatuses(this.t, signatures, opts); }
    /** Submit a signed transaction to the cluster for processing. @since 1.0.0 */
    sendTransaction(signedTx, opts) { return m.sendTransaction(this.t, signedTx, opts); }
    /** Simulate a transaction without submitting it to the cluster. @since 1.0.0 */
    simulateTransaction(tx, opts) { return m.simulateTransaction(this.t, tx, opts); }
    /** Request an airdrop of lamports to a public key (devnet/testnet only). @since 1.0.0 */
    requestAirdrop(pubkey, lamports, commitment, opts) { return m.requestAirdrop(this.t, pubkey, lamports, commitment, opts); }
    // ── Blockhash ──────────────────────────────────────────────────
    /** Get the latest blockhash. @since 1.0.0 */
    getLatestBlockhash(commitment, opts) { return m.getLatestBlockhash(this.t, commitment, opts); }
    /** Check whether a blockhash is still valid. @since 1.0.0 */
    isBlockhashValid(blockhash, commitment, opts) { return m.isBlockhashValid(this.t, blockhash, commitment, opts); }
    // ── Cluster / Epoch ────────────────────────────────────────────
    /** Get the current slot the node is processing. @since 1.0.0 */
    getSlot(commitment, opts) { return m.getSlot(this.t, commitment, opts); }
    /** Get the current slot leader. @since 1.0.0 */
    getSlotLeader(commitment, opts) { return m.getSlotLeader(this.t, commitment, opts); }
    /** Get the slot leaders for a range of slots. @since 1.0.0 */
    getSlotLeaders(startSlot, limit, opts) { return m.getSlotLeaders(this.t, startSlot, limit, opts); }
    /** Get information about the current epoch. @since 1.0.0 */
    getEpochInfo(commitment, opts) { return m.getEpochInfo(this.t, commitment, opts); }
    /** Get the epoch schedule. @since 1.0.0 */
    getEpochSchedule(opts) { return m.getEpochSchedule(this.t, opts); }
    /** Get the current inflation governor parameters. @since 1.0.0 */
    getInflationGovernor(commitment, opts) { return m.getInflationGovernor(this.t, commitment, opts); }
    /** Get the specific inflation values for the current epoch. @since 1.0.0 */
    getInflationRate(opts) { return m.getInflationRate(this.t, opts); }
    /** Get inflation / staking rewards for a list of addresses. @since 1.0.0 */
    getInflationReward(addresses, opts) { return m.getInflationReward(this.t, addresses, opts); }
    /** Get account info and associated stake for all voting accounts. @since 1.0.0 */
    getVoteAccounts(opts) { return m.getVoteAccounts(this.t, opts); }
    /** Get information about all the nodes participating in the cluster. @since 1.0.0 */
    getClusterNodes(opts) { return m.getClusterNodes(this.t, opts); }
    /** Get information about the current supply of SOL. @since 1.0.0 */
    getSupply(opts) { return m.getSupply(this.t, opts); }
    /** Get a list of recent performance samples. @since 1.0.0 */
    getRecentPerformanceSamples(limit, opts) { return m.getRecentPerformanceSamples(this.t, limit, opts); }
    /** Check the health of the node ("ok" if healthy). @since 1.0.0 */
    getHealth(opts) { return m.getHealth(this.t, opts); }
    /** Get the current Solana version running on the node. @since 1.0.0 */
    getVersion(opts) { return m.getVersion(this.t, opts); }
    /** Get the genesis hash. @since 1.0.0 */
    getGenesisHash(opts) { return m.getGenesisHash(this.t, opts); }
    /** Get the identity public key for the current node. @since 1.0.0 */
    getIdentity(opts) { return m.getIdentity(this.t, opts); }
    /** Get the minimum balance required to make an account rent-exempt. @since 1.0.0 */
    getMinimumBalanceForRentExemption(dataLength, commitment, opts) { return m.getMinimumBalanceForRentExemption(this.t, dataLength, commitment, opts); }
    /** Get the lowest slot that the node has information about in its ledger. @since 1.0.0 */
    minimumLedgerSlot(opts) { return m.minimumLedgerSlot(this.t, opts); }
    /** Get the slot of the lowest confirmed block not yet purged from the ledger. @since 1.0.0 */
    getFirstAvailableBlock(opts) { return m.getFirstAvailableBlock(this.t, opts); }
    /** Get the highest slot information that the node has snapshots for. @since 1.0.0 */
    getHighestSnapshotSlot(opts) { return m.getHighestSnapshotSlot(this.t, opts); }
    /** Get the leader schedule for an epoch. @since 1.0.0 */
    getLeaderSchedule(slot, opts) { return m.getLeaderSchedule(this.t, slot, opts); }
    /** Get the max slot seen from retransmit stage. @since 1.0.0 */
    getMaxRetransmitSlot(opts) { return m.getMaxRetransmitSlot(this.t, opts); }
    /** Get the max slot seen from after shred insert. @since 1.0.0 */
    getMaxShredInsertSlot(opts) { return m.getMaxShredInsertSlot(this.t, opts); }
    /** Get the current transaction count from the ledger. @since 1.0.0 */
    getTransactionCount(commitment, opts) { return m.getTransactionCount(this.t, commitment, opts); }
    /** Get the stake minimum delegation in lamports. @since 1.0.0 */
    getStakeMinimumDelegation(commitment, opts) { return m.getStakeMinimumDelegation(this.t, commitment, opts); }
    /** Get a list of recent prioritization fees observed by the node. @since 1.0.0 */
    getRecentPrioritizationFees(addresses, opts) { return m.getRecentPrioritizationFees(this.t, addresses, opts); }
    // ── Token ──────────────────────────────────────────────────────
    /** Get the token balance of an SPL Token account. @since 1.0.0 */
    getTokenAccountBalance(tokenAccount, commitment, opts) { return m.getTokenAccountBalance(this.t, tokenAccount, commitment, opts); }
    /** Get all SPL Token accounts by token owner. @since 1.0.0 */
    getTokenAccountsByOwner(owner, filter, opts) { return m.getTokenAccountsByOwner(this.t, owner, filter, opts); }
    /** Get all SPL Token accounts by approved delegate. @since 1.0.0 */
    getTokenAccountsByDelegate(delegate, filter, opts) { return m.getTokenAccountsByDelegate(this.t, delegate, filter, opts); }
    /** Get the 20 largest accounts of a particular SPL Token type. @since 1.0.0 */
    getTokenLargestAccounts(mint, commitment, opts) { return m.getTokenLargestAccounts(this.t, mint, commitment, opts); }
    /** Get the total supply of an SPL Token type. @since 1.0.0 */
    getTokenSupply(mint, commitment, opts) { return m.getTokenSupply(this.t, mint, commitment, opts); }
    /** Get the 20 largest accounts by lamport balance. @since 1.0.0 */
    getLargestAccounts(opts) { return m.getLargestAccounts(this.t, opts); }
    /** @deprecated Deprecated in solana-core v2.0. @since 1.0.0 */
    getStakeActivation(stakeAccount, opts) { return m.getStakeActivation(this.t, stakeAccount, opts); }
    /** Get the fee the network will charge for a particular message. @since 1.0.0 */
    getFeeForMessage(message, commitment, opts) { return m.getFeeForMessage(this.t, message, commitment, opts); }
}
//# sourceMappingURL=solana-rpc.js.map